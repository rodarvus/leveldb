<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>
<!-- Plugin "leveldb" - A leveling database for Aardwolf -->

<muclient>
<plugin
   name="leveldb"
   author="Rodarvus"
   id="b34c04e52c6c7bced4508230"
   language="Lua"
   purpose="A simple leveling database for Aardwolf"
   date_written="2026-02-17 08:09:56"
   requires="5.07"
   version="9.0"
   save_state="y"
   >

</plugin>


<!--  Aliases  -->

<aliases>
   <alias
      match="^ldb$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_status"
   ></alias>
   <alias
      match="^ldb help$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_help"
   ></alias>
   <alias
      match="^ldb on$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_on"
   ></alias>
   <alias
      match="^ldb off$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_off"
   ></alias>
   <alias
      match="^ldb level(?: (\d+))?(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_level"
   ></alias>
   <alias
      match="^ldb this(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_thislevel"
   ></alias>
   <alias
      match="^ldb last(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_lastlevel"
   ></alias>
   <alias
      match="^ldb zone(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_zone"
   ></alias>
   <alias
      match="^ldb mob (.+)$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_mob"
   ></alias>
   <alias
      match="^ldb top mobs(?: (\d+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_top_mobs"
   ></alias>
   <alias
      match="^ldb top zones(?: (\d+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_top_zones"
   ></alias>
   <alias
      match="^ldb top xp(?: (\d+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_top_xp"
   ></alias>
   <alias
      match="^ldb deaths(?: (\d+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_deaths"
   ></alias>
   <alias
      match="^ldb remort(.*)$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_remort"
   ></alias>
   <alias
      match="^ldb tier(.*)$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_tier"
   ></alias>
   <alias
      match="^ldb quests?(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_quests"
   ></alias>
   <alias
      match="^ldb (?:cps?|campaigns?)(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_campaigns"
   ></alias>
   <alias
      match="^ldb (?:cps?|campaigns?) show (\d+)$"
      enabled="y"
      regexp="y"
      sequence="99"
      script="ldb_campaign_show"
   ></alias>
   <alias
      match="^ldb pup(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_pup"
   ></alias>
   <alias
      match="^ldb gq(?:uest)?s? show (\d+)$"
      enabled="y"
      regexp="y"
      sequence="99"
      script="ldb_gquest_show"
   ></alias>
   <alias
      match="^ldb gq(?:uest)?s?(?: (.+))?$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_gquests"
   ></alias>
   <alias
      match="^ldb db$"
      enabled="y"
      regexp="y"
      sequence="100"
      script="ldb_db_info"
   ></alias>
</aliases>


<!-- Triggers -->

<triggers>
   <!-- Player damage line: [optional hits] Your spell VERB mob. [damage] -->
   <!-- Optional leading * for crits, optional [N] hits prefix, must start with "Your" -->
   <!-- Mob damage to player uses possessive mob name, not "Your", so won't match -->
   <trigger
      match="^\*?(?:\[\d+\] )?Your .+[.!] \[(\d+)\]\*?$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_damage_line"
      sequence="100"
   ></trigger>

   <!-- Player death -->
   <trigger
      match="^You die\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_player_death"
      sequence="100"
   ></trigger>

   <!-- Sacrifice message: deity gives gold for corpse. Mob level = gold * 2 -->
   <trigger
      match="^.+ gives you (\d+) gold coins? for .*\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_sacrifice"
      sequence="100"
   ></trigger>

   <!-- Powerup detection trigger -->
   <trigger
      name="trg_ldb_powerup"
      match="^Congratulations, .+\. You have increased your powerups to (\d+)\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_powerup"
      sequence="100"
   ></trigger>
   <!-- Powerup train rewards (always-on, fire right after powerup message) -->
   <trigger
      name="trg_ldb_pup_trains"
      match="^You gain (\d+) trains?\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_pup_trains"
      sequence="100"
   ></trigger>
   <trigger
      name="trg_ldb_pup_trains_lucky"
      match="^Lucky! You gain an extra (\d+) training sessions?!$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_pup_trains"
      sequence="100"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Campaign triggers (always-on, don't gag)                     -->
   <!-- ============================================================ -->

   <!-- Campaign start: questmaster says "Good luck" -->
   <trigger
      name="trg_ldb_cp_start"
      match="^(.+) tells you 'Good luck in your campaign!'$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_start"
      sequence="100"
   ></trigger>

   <!-- Campaign mob killed -->
   <trigger
      name="trg_ldb_cp_mob_killed"
      match="^Congratulations, that was one of your CAMPAIGN mobs!$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_mob_killed"
      sequence="100"
   ></trigger>

   <!-- Campaign completed -->
   <trigger
      name="trg_ldb_cp_complete"
      match="CONGRATULATIONS! You have completed your campaign."
      enabled="y"
      regexp="n"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_complete"
      sequence="100"
   ></trigger>

   <!-- Campaign quit -->
   <trigger
      name="trg_ldb_cp_quit"
      match="^Campaign cleared\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_quit"
      sequence="100"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Silent cp info parsing triggers (disabled, gag output)       -->
   <!-- ============================================================ -->

   <!-- No active campaign response -->
   <trigger
      name="trg_ldb_cpinfo_none"
      match="^You are not currently on a campaign\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_none"
      sequence="89"
   ></trigger>

   <!-- cp info: Level Taken -->
   <trigger
      name="trg_ldb_cpinfo_level"
      match="^Level Taken\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_level"
      sequence="89"
   ></trigger>

   <!-- cp info: Quest Points -->
   <trigger
      name="trg_ldb_cpinfo_qp"
      match="^Quest Points\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_qp"
      sequence="89"
   ></trigger>

   <!-- cp info: Gold Coins -->
   <trigger
      name="trg_ldb_cpinfo_gold"
      match="^Gold Coins\.+:\s*\[\s*([\d,]+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_gold"
      sequence="89"
   ></trigger>

   <!-- cp info: mob line -->
   <trigger
      name="trg_ldb_cpinfo_mob"
      match="^Find and kill \d+ \* (.+) \((.+)\)$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_mob"
      sequence="89"
   ></trigger>

   <!-- cp info: "Use 'cp check'" line (true last line of cp info) -->
   <trigger
      name="trg_ldb_cpinfo_end"
      match="^Use 'cp check'"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_end"
      sequence="91"
   ></trigger>

   <!-- cp info: catch-all gag for header/metadata/other lines -->
   <trigger
      name="trg_ldb_cpinfo_gag"
      match="^(?:-+\[|Complete By|Time Left|Training Session|Practice|Trivia|The targets for|-{20,}|$)"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_cpinfo_gag"
      sequence="90"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Campaign reward parsing triggers (disabled, don't gag)       -->
   <!-- ============================================================ -->

   <!-- Reward line -->
   <trigger
      name="trg_ldb_cp_reward"
      match="^\s*Reward of ([\d,]+) (.+) added\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_reward"
      sequence="90"
   ></trigger>

   <!-- Closing separator (end of reward block) -->
   <trigger
      name="trg_ldb_cp_reward_end"
      match="^-{20,}$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_cp_reward_end"
      sequence="90"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Global Quest triggers (always-on, don't gag)                 -->
   <!-- ============================================================ -->

   <!-- GQ join -->
   <trigger
      name="trg_ldb_gq_join"
      match="^You have now joined Global Quest # (\d+)\."
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_join"
      sequence="100"
   ></trigger>

   <!-- GQ mob killed -->
   <trigger
      name="trg_ldb_gq_mob_killed"
      match="^Congratulations, that was one of the GLOBAL QUEST mobs!$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_mob_killed"
      sequence="100"
   ></trigger>

   <!-- GQ per-mob QP reward (disabled, enabled for 1s after mob kill) -->
   <trigger
      name="trg_ldb_gq_mob_qp"
      match="^(\d+) quest points? awarded\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_mob_qp"
      sequence="100"
   ></trigger>

   <!-- GQ won (first to complete) -->
   <trigger
      name="trg_ldb_gq_won"
      match="^You were the first to complete this quest!$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_won"
      sequence="100"
   ></trigger>

   <!-- GQ finished (completed but not first, during extended mode) -->
   <trigger
      name="trg_ldb_gq_finished"
      match="^You have finished this global quest\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_finished"
      sequence="100"
   ></trigger>

   <!-- GQ ended (expired/over) -->
   <trigger
      name="trg_ldb_gq_ended"
      match="^Global Quest: Global quest # (\d+)(?: \(extended\))? is now over\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_ended"
      sequence="100"
   ></trigger>

   <!-- GQ cancelled -->
   <trigger
      name="trg_ldb_gq_cancelled"
      match="^Global Quest: Global quest # (\d+) has been cancelled due to lack of activity\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_cancelled"
      sequence="100"
   ></trigger>

   <!-- GQ quit -->
   <trigger
      name="trg_ldb_gq_quit"
      match="^You are no longer part of Global Quest # (\d+) and will be unable to rejoin\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_quit"
      sequence="100"
   ></trigger>

   <!-- GQ not in (two patterns, same handler) -->
   <trigger
      name="trg_ldb_gq_not_in_1"
      match="^You are not in a global quest\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_not_in"
      sequence="100"
   ></trigger>
   <trigger
      name="trg_ldb_gq_not_in_2"
      match="^There are no global quests running\.$"
      enabled="y"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_not_in"
      sequence="100"
   ></trigger>

   <!-- ============================================================ -->
   <!-- GQ reward parsing triggers (disabled, don't gag)             -->
   <!-- ============================================================ -->

   <trigger
      name="trg_ldb_gq_reward"
      match="^\s*Reward of ([\d,]+) (.+) added\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="n"
      script="ldb_on_gq_reward"
      sequence="100"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Silent gq info parsing triggers (disabled, gag output)       -->
   <!-- ============================================================ -->

   <!-- No active gquest responses -->
   <trigger
      name="trg_ldb_gqinfo_none"
      match="^You are not in a global quest\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_none"
      sequence="89"
   ></trigger>
   <trigger
      name="trg_ldb_gqinfo_norun"
      match="^There are no global quests running\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_none"
      sequence="89"
   ></trigger>
   <trigger
      name="trg_ldb_gqinfo_multi"
      match="^There is more than one global quest running\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_multi"
      sequence="89"
   ></trigger>

   <!-- gq info: Quest Name (captures gq number) -->
   <trigger
      name="trg_ldb_gqinfo_qn"
      match="^Quest Name\.+:\s*\[\s*Global quest # (\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_qn"
      sequence="89"
   ></trigger>

   <!-- gq info: Quest Status -->
   <trigger
      name="trg_ldb_gqinfo_status"
      match="^Quest Status\.+:\s*\[\s*(\w+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_status"
      sequence="89"
   ></trigger>

   <!-- gq info: Quest Points -->
   <trigger
      name="trg_ldb_gqinfo_qp"
      match="^Quest points\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_qp"
      sequence="89"
   ></trigger>

   <!-- gq info: Gold Coins -->
   <trigger
      name="trg_ldb_gqinfo_gold"
      match="^Gold Coins\.+:\s*\[\s*([\d,]+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_gold"
      sequence="89"
   ></trigger>

   <!-- gq info: Practice Sessions -->
   <trigger
      name="trg_ldb_gqinfo_pracs"
      match="^Practice Sessions\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_pracs"
      sequence="89"
   ></trigger>

   <!-- gq info: Training Sessions -->
   <trigger
      name="trg_ldb_gqinfo_trains"
      match="^Training Sessions?\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_trains"
      sequence="89"
   ></trigger>

   <!-- gq info: Trivia points -->
   <trigger
      name="trg_ldb_gqinfo_tp"
      match="^Trivia points\.+:\s*\[\s*(\d+)\s*\]"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_tp"
      sequence="89"
   ></trigger>

   <!-- gq info: catch-all gag for header/metadata lines -->
   <trigger
      name="trg_ldb_gqinfo_gag"
      match="^(?:-+\[|Quest (?:Name|Type|Status|Duration)|Level range|Time (?:to Start|Expired|Remaining)|Inactivity Timer|Kill at least|Use syntax: 'gquest)"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_gag"
      sequence="90"
   ></trigger>

   <!-- gq info: closing separator (end of gq info output) -->
   <trigger
      name="trg_ldb_gqinfo_end"
      match="^-{20,}$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqinfo_end"
      sequence="91"
   ></trigger>

   <!-- ============================================================ -->
   <!-- Silent gq check parsing triggers (disabled, gag output)      -->
   <!-- ============================================================ -->

   <!-- gq check: mob line (format 1: before gquest starts) -->
   <!-- [* 1] Jille                 - beer       -->
   <!-- [* 4] 2 * billows of smoke  - fireswamp  -->
   <trigger
      name="trg_ldb_gqcheck_mob"
      match="^\[\*?\s*\d+\]\s+(?:(\d+) \* )?(.+?)\s+-\s+(\w+)\s*$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqcheck_mob"
      sequence="89"
   ></trigger>
   <!-- gq check: mob line (format 2: after gquest starts) -->
   <!-- You still have to kill 2 * a sinister vandal (The Three Pillars of Diatz) -->
   <!-- You still have to kill 1 * a mutated goat (The Killing Fields - Dead)     -->
   <trigger
      name="trg_ldb_gqcheck_mob2"
      match="^You still have to kill (\d+) \* (.+) \((.+?)(?:\s*-\s*Dead)?\)\s*$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqcheck_mob2"
      sequence="89"
   ></trigger>

   <!-- gq check: not in gquest -->
   <trigger
      name="trg_ldb_gqcheck_none"
      match="^You are not in a global quest\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqcheck_none"
      sequence="89"
   ></trigger>
   <trigger
      name="trg_ldb_gqcheck_norun"
      match="^There are no global quests running\.$"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqcheck_none"
      sequence="89"
   ></trigger>

   <!-- gq check: header/separator/footer gag -->
   <trigger
      name="trg_ldb_gqcheck_gag"
      match="^(?:Index\s+Target|-{20,}|Note: Dead means|You may win|You can be rewarded)"
      enabled="n"
      regexp="y"
      keep_evaluating="y"
      omit_from_output="y"
      script="ldb_on_gqcheck_gag"
      sequence="90"
   ></trigger>
</triggers>


<script>
<![CDATA[
require "gmcphelper"

-- =============================================================================
-- Constants
-- =============================================================================
local GMCP_HANDLER_ID = "3e7dedbe37e44942dd46d264"
local DB_DIR = "state\\leveldb\\"
local DB_FILENAME = "leveldb.db"
local PREFIX = "[LevelDB] "

-- =============================================================================
-- Plugin state (persisted via save_state)
-- =============================================================================
local enabled = false

-- =============================================================================
-- Database state
-- =============================================================================
local db = nil              -- SQLite database handle
local db_path = nil         -- Full path to database

-- =============================================================================
-- GMCP cached values (updated on each broadcast)
-- =============================================================================
local cached_tier = nil
local cached_remorts = nil
local cached_redos = nil
local cached_pups = nil
local cached_perlevel = 1000
local cached_level = nil
local cached_tnl = nil
local cached_zone = nil
local cached_room_num = nil
local cached_room_name = nil
local cached_enemy = ""
-- =============================================================================
-- Pup tracking state
-- =============================================================================
local last_pup_event_id = nil    -- DB id of most recent pup_events row (for train UPDATE)

-- =============================================================================
-- Quest/Campaign tracking state
-- =============================================================================
local active_quest_id = nil        -- DB id of open quest (nil if none)
local active_campaign_id = nil     -- DB id of open campaign (nil if none)
local cpinfo_mob_buffer = {}       -- temp buffer for mob list during cp info parse
local cpinfo_level = nil           -- Level Taken from cp info (for matching)
local cpinfo_qp = nil              -- Quest Points from cp info (for matching)
local cpinfo_gold = nil            -- Gold Coins from cp info

-- =============================================================================
-- Global Quest tracking state
-- =============================================================================
local active_gquest_id = nil       -- DB id of open gquest (nil if none)
local gqinfo_mob_buffer = {}       -- temp buffer for mob list during gq check parse
local gqinfo_qp = nil              -- base QP from gq info
local gqinfo_gold = nil            -- gold from gq info
local gqinfo_gq_number = nil       -- gq number from gq info (for reconnect matching)
local gqinfo_status = nil          -- Quest Status from gq info (Preparing/Active/Running/Extended/Finished)
local gqinfo_pracs = nil           -- Practice Sessions from gq info
local gqinfo_trains = nil          -- Training Sessions from gq info
local gqinfo_tp = nil              -- Trivia points from gq info

-- =============================================================================
-- Combat tracking state
-- =============================================================================
local in_combat = false
local death_flag = false
local fight_start = nil     -- Snapshot at combat start: {tnl, level, zone, room_num, room_name, enemy, pup}
local damage_total = 0      -- Accumulated damage from [damage] lines this fight
local round_count = 0       -- Number of enemypct changes this fight
local last_enemypct = nil   -- For dedup of multiple char.status per round
local sacrifice_mob_level = nil  -- Estimated mob level from sacrifice gold * 2

-- =============================================================================
-- Database helpers (following aard_GMCP_mapper.xml patterns)
-- =============================================================================
local function dbcheck(code, msg, query)
   if code ~= sqlite3.OK and
      code ~= sqlite3.ROW and
      code ~= sqlite3.DONE then
      local err = (msg or "unknown") .. "\nCODE: " .. (code or "?") .. "\nQUERY: " .. (query or "?")
      if db then db:exec("ROLLBACK") end
      ColourNote("red", "", PREFIX .. "SQL Error: " .. err)
      return true  -- error occurred
   end
   return false  -- no error
end

local function fixsql(s)
   if s then
      return "'" .. (string.gsub(s, "'", "''")) .. "'"
   else
      return "NULL"
   end
end

-- =============================================================================
-- Schema initialization (split into tables, migrations, indexes)
-- =============================================================================
local function init_tables()
   local sql = [[
      CREATE TABLE IF NOT EXISTS kills (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp    INTEGER NOT NULL,
         mob_name     TEXT NOT NULL,
         zone         TEXT NOT NULL,
         room_num     INTEGER,
         room_name    TEXT,
         level        INTEGER NOT NULL,
         xp_gained    INTEGER NOT NULL DEFAULT 0,
         damage_total INTEGER NOT NULL DEFAULT 0,
         rounds       INTEGER NOT NULL DEFAULT 0,
         tier         INTEGER,
         remort       INTEGER,
         pup          INTEGER,
         mob_level    INTEGER
      );

      CREATE TABLE IF NOT EXISTS deaths (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp    INTEGER NOT NULL,
         mob_name     TEXT,
         zone         TEXT NOT NULL,
         room_num     INTEGER,
         room_name    TEXT,
         level        INTEGER NOT NULL,
         tier         INTEGER,
         remort       INTEGER
      );

      CREATE TABLE IF NOT EXISTS quests (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp    INTEGER NOT NULL,
         level        INTEGER NOT NULL,
         tier         INTEGER,
         remort       INTEGER,
         mob_name     TEXT,
         area         TEXT,
         room         TEXT,
         timer        INTEGER,
         result       TEXT,
         duration     INTEGER,
         qp           INTEGER,
         gold         INTEGER,
         tp           INTEGER,
         trains       INTEGER,
         pracs        INTEGER
      );

      CREATE TABLE IF NOT EXISTS campaigns (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp    INTEGER NOT NULL,
         level        INTEGER NOT NULL,
         tier         INTEGER,
         remort       INTEGER,
         result       TEXT,
         duration     INTEGER,
         mob_count    INTEGER,
         qp           INTEGER,
         gold         INTEGER,
         tp           INTEGER,
         trains       INTEGER,
         pracs        INTEGER
      );

      CREATE TABLE IF NOT EXISTS campaign_mobs (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         campaign_id  INTEGER NOT NULL REFERENCES campaigns(id),
         mob_name     TEXT NOT NULL,
         area         TEXT
      );

      CREATE TABLE IF NOT EXISTS pup_events (
         id           INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp    INTEGER NOT NULL,
         tier         INTEGER NOT NULL,
         remort       INTEGER NOT NULL,
         pup_number   INTEGER NOT NULL,
         zone         TEXT,
         trains_earned INTEGER,
         xp_per_level INTEGER
      );

      CREATE TABLE IF NOT EXISTS gquests (
         id            INTEGER PRIMARY KEY AUTOINCREMENT,
         timestamp     INTEGER NOT NULL,
         gq_number     INTEGER,
         level         INTEGER NOT NULL,
         tier          INTEGER,
         remort        INTEGER,
         redo          INTEGER,
         result        TEXT,
         duration      INTEGER,
         mob_count     INTEGER,
         qp            INTEGER,
         gold          INTEGER,
         tp            INTEGER,
         trains        INTEGER,
         pracs         INTEGER,
         expected_qp   INTEGER,
         expected_gold INTEGER
      );

      CREATE TABLE IF NOT EXISTS gquest_mobs (
         id          INTEGER PRIMARY KEY AUTOINCREMENT,
         gquest_id   INTEGER NOT NULL REFERENCES gquests(id),
         mob_name    TEXT NOT NULL,
         area        TEXT,
         mob_count   INTEGER DEFAULT 1
      );
   ]]
   db:exec(sql)
   return not dbcheck(db:errcode(), db:errmsg(), "init_tables")
end

local function run_migrations()
   -- Migration: add pup column (v2.0 -> v3.0)
   db:exec("ALTER TABLE kills ADD COLUMN pup INTEGER")
   -- Migration: add mob_level column (v3.0 -> v4.0)
   db:exec("ALTER TABLE kills ADD COLUMN mob_level INTEGER")
   -- Migration: add expected_qp/expected_gold to campaigns (v6.0)
   db:exec("ALTER TABLE campaigns ADD COLUMN expected_qp INTEGER")
   db:exec("ALTER TABLE campaigns ADD COLUMN expected_gold INTEGER")
   -- Migration: add combat_time column (v6.0 -> v7.0)
   db:exec("ALTER TABLE kills ADD COLUMN combat_time REAL")
   -- Migration: add redo column (v7.0 -> v8.0)
   db:exec("ALTER TABLE kills ADD COLUMN redo INTEGER")
   db:exec("ALTER TABLE deaths ADD COLUMN redo INTEGER")
   db:exec("ALTER TABLE quests ADD COLUMN redo INTEGER")
   db:exec("ALTER TABLE campaigns ADD COLUMN redo INTEGER")
   db:exec("ALTER TABLE pup_events ADD COLUMN redo INTEGER")
   -- Errors from "duplicate column name" are silently ignored
end

local function init_indexes()
   local sql = [[
      CREATE INDEX IF NOT EXISTS idx_kills_level ON kills(level);
      CREATE INDEX IF NOT EXISTS idx_kills_zone ON kills(zone);
      CREATE INDEX IF NOT EXISTS idx_kills_mob ON kills(mob_name);
      CREATE INDEX IF NOT EXISTS idx_kills_timestamp ON kills(timestamp);
      CREATE INDEX IF NOT EXISTS idx_kills_tier ON kills(tier);
      CREATE INDEX IF NOT EXISTS idx_kills_remort ON kills(remort);
      CREATE INDEX IF NOT EXISTS idx_kills_pup ON kills(pup);
      CREATE INDEX IF NOT EXISTS idx_kills_mob_level ON kills(mob_level);
      CREATE INDEX IF NOT EXISTS idx_deaths_level ON deaths(level);
      CREATE INDEX IF NOT EXISTS idx_deaths_zone ON deaths(zone);
      CREATE INDEX IF NOT EXISTS idx_deaths_tier ON deaths(tier);
      CREATE INDEX IF NOT EXISTS idx_deaths_remort ON deaths(remort);
      CREATE INDEX IF NOT EXISTS idx_quests_level ON quests(level);
      CREATE INDEX IF NOT EXISTS idx_quests_tier ON quests(tier);
      CREATE INDEX IF NOT EXISTS idx_quests_remort ON quests(remort);
      CREATE INDEX IF NOT EXISTS idx_campaigns_level ON campaigns(level);
      CREATE INDEX IF NOT EXISTS idx_campaigns_tier ON campaigns(tier);
      CREATE INDEX IF NOT EXISTS idx_campaigns_remort ON campaigns(remort);
      CREATE INDEX IF NOT EXISTS idx_campaign_mobs_cid ON campaign_mobs(campaign_id);
      CREATE INDEX IF NOT EXISTS idx_pup_events_tier_remort ON pup_events(tier, remort);
      CREATE INDEX IF NOT EXISTS idx_kills_redo ON kills(redo);
      CREATE INDEX IF NOT EXISTS idx_deaths_redo ON deaths(redo);
      CREATE INDEX IF NOT EXISTS idx_quests_redo ON quests(redo);
      CREATE INDEX IF NOT EXISTS idx_campaigns_redo ON campaigns(redo);
      CREATE INDEX IF NOT EXISTS idx_pup_events_redo ON pup_events(redo);
      CREATE INDEX IF NOT EXISTS idx_gquests_level ON gquests(level);
      CREATE INDEX IF NOT EXISTS idx_gquests_tier ON gquests(tier);
      CREATE INDEX IF NOT EXISTS idx_gquests_remort ON gquests(remort);
      CREATE INDEX IF NOT EXISTS idx_gquests_redo ON gquests(redo);
      CREATE INDEX IF NOT EXISTS idx_gquest_mobs_gqid ON gquest_mobs(gquest_id);
   ]]
   db:exec(sql)
   return not dbcheck(db:errcode(), db:errmsg(), "init_indexes")
end

-- =============================================================================
-- Database open/close
-- =============================================================================
local function close_db()
   if db then
      db:close()
      db = nil
      db_path = nil
   end
end

local function open_db()
   if db then return true end

   local dir = GetInfo(60) .. DB_DIR
   os.execute('if not exist "' .. dir .. '" mkdir "' .. dir .. '"')
   local path = dir .. DB_FILENAME

   local handle, err_msg, err_code = sqlite3.open(path)
   if not handle then
      ColourNote("red", "", PREFIX .. "Failed to open database: " .. (err_msg or "unknown error"))
      return false
   end

   db = handle
   db_path = path

   if not init_tables() then
      close_db()
      return false
   end

   run_migrations()

   if not init_indexes() then
      close_db()
      return false
   end

   return true
end

-- =============================================================================
-- Combat state management
-- =============================================================================
local function start_fight(enemy_name)
   in_combat = true
   death_flag = false
   damage_total = 0
   round_count = 0
   last_enemypct = nil
   last_pup_event_id = nil  -- pup reward lines are done once combat resumes
   sacrifice_mob_level = nil

   fight_start = {
      tnl = cached_tnl,
      level = cached_level,
      zone = cached_zone or "Unknown",
      room_num = cached_room_num,
      room_name = cached_room_name,
      enemy = enemy_name,
      pup = cached_pups,
      start_time = utils.timer(),
   }
end

local function record_kill()
   if not fight_start then return end
   if not db then return end
   if not enabled then return end

   -- Calculate XP gained
   local xp_gained = 0
   if fight_start.tnl and cached_tnl and fight_start.level and cached_level then
      if cached_level > fight_start.level then
         -- Leveled up during combat (1-199)
         xp_gained = fight_start.tnl + (cached_perlevel - cached_tnl)
      elseif fight_start.level >= 200 and cached_tnl > fight_start.tnl then
         -- Powerup during combat (200/201: TNL reset without level change)
         xp_gained = fight_start.tnl + (cached_perlevel - cached_tnl)
      elseif cached_level == fight_start.level then
         -- Normal kill (same level/pup, no reset)
         xp_gained = fight_start.tnl - cached_tnl
      end
      if xp_gained < 0 then xp_gained = 0 end
   end

   local combat_time = nil
   if fight_start.start_time then
      combat_time = utils.timer() - fight_start.start_time
      if combat_time < 0 then combat_time = nil end
   end

   local query = string.format(
      "INSERT INTO kills (timestamp, mob_name, zone, room_num, room_name, level, " ..
      "xp_gained, damage_total, rounds, tier, remort, pup, mob_level, combat_time, redo) " ..
      "VALUES (%d, %s, %s, %s, %s, %d, %d, %d, %d, %s, %s, %s, %s, %s, %s)",
      os.time(),
      fixsql(fight_start.enemy),
      fixsql(fight_start.zone),
      fight_start.room_num and tostring(fight_start.room_num) or "NULL",
      fixsql(fight_start.room_name),
      fight_start.level or 0,
      xp_gained,
      damage_total,
      round_count,
      cached_tier and tostring(cached_tier) or "NULL",
      cached_remorts and tostring(cached_remorts) or "NULL",
      fight_start.pup and tostring(fight_start.pup) or "NULL",
      sacrifice_mob_level and tostring(sacrifice_mob_level) or "NULL",
      combat_time and string.format("%.3f", combat_time) or "NULL",
      cached_redos and tostring(cached_redos) or "NULL"
   )

   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
end

local function record_death()
   if not db then return end
   if not enabled then return end

   local enemy = (in_combat and fight_start) and fight_start.enemy or cached_enemy

   local query = string.format(
      "INSERT INTO deaths (timestamp, mob_name, zone, room_num, room_name, level, tier, remort, redo) " ..
      "VALUES (%d, %s, %s, %s, %s, %d, %s, %s, %s)",
      os.time(),
      fixsql(enemy),
      fixsql(cached_zone or "Unknown"),
      cached_room_num and tostring(cached_room_num) or "NULL",
      fixsql(cached_room_name),
      cached_level or 0,
      cached_tier and tostring(cached_tier) or "NULL",
      cached_remorts and tostring(cached_remorts) or "NULL",
      cached_redos and tostring(cached_redos) or "NULL"
   )

   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
end

local function record_pup_event(pup_data, trains_earned)
   if not db then return end
   if not enabled then return end

   local query = string.format(
      "INSERT INTO pup_events (timestamp, tier, remort, pup_number, zone, " ..
      "trains_earned, xp_per_level, redo) " ..
      "VALUES (%d, %s, %s, %d, %s, %s, %s, %s)",
      pup_data.timestamp,
      pup_data.tier and tostring(pup_data.tier) or "0",
      pup_data.remort and tostring(pup_data.remort) or "0",
      pup_data.pup_number,
      fixsql(pup_data.zone),
      trains_earned and tostring(trains_earned) or "NULL",
      pup_data.xp_per_level and tostring(pup_data.xp_per_level) or "NULL",
      pup_data.redo and tostring(pup_data.redo) or "NULL"
   )

   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
end

local function end_combat()
   in_combat = false
   fight_start = nil
   damage_total = 0
   round_count = 0
   last_enemypct = nil
   sacrifice_mob_level = nil
end


-- =============================================================================
-- GMCP broadcast handler
-- =============================================================================
function OnPluginBroadcast(msg, id, name, text)
   if id ~= GMCP_HANDLER_ID then return end

   if text == "char.base" then
      local base = gmcp("char.base")
      if base then
         if base.perlevel then cached_perlevel = tonumber(base.perlevel) or 1000 end
         if base.tier then cached_tier = tonumber(base.tier) end
         if base.remorts then cached_remorts = tonumber(base.remorts) end
         if base.redos then cached_redos = tonumber(base.redos) end
         if base.pups then cached_pups = tonumber(base.pups) end
      end

   elseif text == "char.status" then
      local status = gmcp("char.status")
      if not status then return end

      -- Update cached values
      local new_level = tonumber(status.level)
      local new_tnl = tonumber(status.tnl)
      if new_level and new_level ~= -1 then cached_level = new_level end
      if new_tnl and new_tnl ~= -1 then cached_tnl = new_tnl end

      local new_enemy = status.enemy or ""

      -- State machine transitions
      if not enabled then
         cached_enemy = new_enemy
         return
      end

      if not in_combat then
         -- IDLE state
         if new_enemy ~= "" then
            -- Transition 1: IDLE --> COMBAT
            start_fight(new_enemy)
         end
      else
         -- COMBAT state
         if death_flag then
            if new_enemy == "" then
               end_combat()
            end
            cached_enemy = new_enemy
            return
         end

         if new_enemy == "" then
            -- Transition 3: COMBAT --> KILL_RECORDED --> IDLE
            record_kill()
            end_combat()

         elseif new_enemy ~= cached_enemy and cached_enemy ~= "" then
            -- Transition 4: COMBAT --> KILL_RECORDED --> COMBAT (enemy switch)
            record_kill()
            start_fight(new_enemy)

         else
            -- Transition 2: COMBAT --> COMBAT (same enemy)
            local new_pct = tonumber(status.enemypct)
            if new_pct and last_enemypct and new_pct > last_enemypct then
               -- Health went UP with same name = previous mob died, new mob engaged
               record_kill()
               start_fight(new_enemy)
            elseif new_pct and new_pct ~= last_enemypct then
               round_count = round_count + 1
               last_enemypct = new_pct
            end
         end
      end

      cached_enemy = new_enemy

   elseif text == "room.info" then
      local room = gmcp("room.info")
      if room then
         cached_zone = room.zone
         cached_room_num = tonumber(room.num)
         cached_room_name = room.name
      end

   elseif text == "comm.quest" then
      local quest_data = gmcp("comm.quest")
      if quest_data then
         handle_quest(quest_data)
      end
   end
end

-- =============================================================================
-- Text trigger handlers
-- =============================================================================
function ldb_on_damage_line(name, line, wildcards)
   if not in_combat then return end
   if not enabled then return end

   local dmg = tonumber(wildcards[1])
   if dmg then
      damage_total = damage_total + dmg
   end
end

function ldb_on_player_death(name, line, wildcards)
   if not enabled then return end
   if death_flag then return end  -- Guard against double-fire

   death_flag = true
   record_death()
end

function ldb_on_sacrifice(name, line, wildcards)
   if not enabled then return end

   local gold = tonumber(wildcards[1])
   if gold then
      sacrifice_mob_level = gold * 2
   end
end

function ldb_on_powerup(name, line, wildcards)
   if not enabled then return end
   if not db then return end

   local pup_number = tonumber(wildcards[1])
   if not pup_number then return end

   -- Update cached pups
   cached_pups = pup_number

   -- Record pup event immediately with trains_earned = 0
   -- Train text triggers will UPDATE this record to add trains
   record_pup_event({
      timestamp = os.time(),
      tier = cached_tier,
      remort = cached_remorts,
      redo = cached_redos,
      pup_number = pup_number,
      zone = cached_zone or "Unknown",
      xp_per_level = cached_perlevel,
   }, 0)
   last_pup_event_id = db:last_insert_rowid()

   -- Clear last_pup_event_id after 1 second to prevent quest/other train
   -- messages from being attributed to this pup. Pup train lines arrive
   -- in the same server packet (milliseconds), so 1s is generous.
   DoAfterSpecial(1, "last_pup_event_id = nil", 12)  -- 12 = sendto.script
end

function ldb_on_pup_trains(name, line, wildcards)
   if not enabled then return end
   if not db then return end
   if not last_pup_event_id then return end

   local trains = tonumber(wildcards[1])
   if not trains or trains <= 0 then return end

   db:exec(string.format(
      "UPDATE pup_events SET trains_earned = COALESCE(trains_earned, 0) + %d WHERE id = %d",
      trains, last_pup_event_id))
end

-- =============================================================================
-- Quest/Campaign: close open quest/campaign as 'unknown'
-- =============================================================================
local function close_open_quest()
   if not active_quest_id or not db then return end
   local now = os.time()
   db:exec(string.format(
      "UPDATE quests SET result = 'unknown', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_quest_id))
   dbcheck(db:errcode(), db:errmsg(), "close_open_quest")
   active_quest_id = nil
end

local function close_open_campaign()
   if not active_campaign_id or not db then return end
   local now = os.time()
   db:exec(string.format(
      "UPDATE campaigns SET result = 'unknown', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_campaign_id))
   dbcheck(db:errcode(), db:errmsg(), "close_open_campaign")
   active_campaign_id = nil
end

local function close_open_gquest()
   if not active_gquest_id or not db then return end
   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'unknown', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "close_open_gquest")
   active_gquest_id = nil
end

-- =============================================================================
-- Quest/Campaign: cp info silent parsing helpers
-- =============================================================================
local function enable_cpinfo_parsing()
   cpinfo_mob_buffer = {}
   cpinfo_level = nil
   cpinfo_qp = nil
   cpinfo_gold = nil
   EnableTrigger("trg_ldb_cpinfo_none", true)
   EnableTrigger("trg_ldb_cpinfo_level", true)
   EnableTrigger("trg_ldb_cpinfo_qp", true)
   EnableTrigger("trg_ldb_cpinfo_gold", true)
   EnableTrigger("trg_ldb_cpinfo_mob", true)
   EnableTrigger("trg_ldb_cpinfo_end", true)
   EnableTrigger("trg_ldb_cpinfo_gag", true)
end

local function disable_cpinfo_parsing()
   EnableTrigger("trg_ldb_cpinfo_none", false)
   EnableTrigger("trg_ldb_cpinfo_level", false)
   EnableTrigger("trg_ldb_cpinfo_qp", false)
   EnableTrigger("trg_ldb_cpinfo_gold", false)
   EnableTrigger("trg_ldb_cpinfo_mob", false)
   EnableTrigger("trg_ldb_cpinfo_end", false)
   EnableTrigger("trg_ldb_cpinfo_gag", false)
end

local function send_silent_cp_info()
   enable_cpinfo_parsing()
   SendNoEcho("cp info")
end

local function flush_cpinfo_mob_buffer()
   if not active_campaign_id or not db then return end
   if #cpinfo_mob_buffer == 0 then return end

   -- Delete old mobs for this campaign and re-insert
   db:exec(string.format("DELETE FROM campaign_mobs WHERE campaign_id = %d", active_campaign_id))

   for _, mob in ipairs(cpinfo_mob_buffer) do
      local query = string.format(
         "INSERT INTO campaign_mobs (campaign_id, mob_name, area) VALUES (%d, %s, %s)",
         active_campaign_id, fixsql(mob.name), fixsql(mob.area))
      db:exec(query)
      dbcheck(db:errcode(), db:errmsg(), query)
   end

   -- Update mob_count and expected rewards on campaign
   local query = string.format(
      "UPDATE campaigns SET mob_count = (SELECT COUNT(*) FROM campaign_mobs WHERE campaign_id = %d), " ..
      "expected_qp = %s, expected_gold = %s WHERE id = %d",
      active_campaign_id,
      cpinfo_qp and tostring(cpinfo_qp) or "NULL",
      cpinfo_gold and tostring(cpinfo_gold) or "NULL",
      active_campaign_id)
   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
end

-- =============================================================================
-- Quest: GMCP comm.quest handler
-- =============================================================================
function handle_quest(data)
   if not data or not data.action then return end
   if not db then return end
   if not enabled then return end

   local action = data.action

   if action == "start" then
      -- New quest assigned
      close_open_quest()
      local query = string.format(
         "INSERT INTO quests (timestamp, level, tier, remort, mob_name, area, room, timer, redo) " ..
         "VALUES (%d, %d, %s, %s, %s, %s, %s, %s, %s)",
         os.time(),
         cached_level or 0,
         cached_tier and tostring(cached_tier) or "NULL",
         cached_remorts and tostring(cached_remorts) or "NULL",
         fixsql(data.targ),
         fixsql(data.area),
         fixsql(data.room),
         data.timer and tostring(data.timer) or "NULL",
         cached_redos and tostring(cached_redos) or "NULL")
      db:exec(query)
      if not dbcheck(db:errcode(), db:errmsg(), query) then
         active_quest_id = db:last_insert_rowid()
      end

   elseif action == "comp" then
      -- Quest completed
      if active_quest_id then
         local now = os.time()
         local query = string.format(
            "UPDATE quests SET result = 'completed', duration = %d - timestamp, " ..
            "qp = %s, gold = %s, tp = %s, trains = %s, pracs = %s " ..
            "WHERE id = %d AND result IS NULL",
            now,
            tonumber(data.totqp) and tostring(data.totqp) or "NULL",
            tonumber(data.gold) and tostring(data.gold) or "NULL",
            tonumber(data.tp) and tostring(data.tp) or "NULL",
            tonumber(data.trains) and tostring(data.trains) or "NULL",
            tonumber(data.pracs) and tostring(data.pracs) or "NULL",
            active_quest_id)
         db:exec(query)
         dbcheck(db:errcode(), db:errmsg(), query)
         active_quest_id = nil
      end

   elseif action == "fail" then
      if active_quest_id then
         local now = os.time()
         local query = string.format(
            "UPDATE quests SET result = 'failed', duration = %d - timestamp WHERE id = %d AND result IS NULL",
            now, active_quest_id)
         db:exec(query)
         dbcheck(db:errcode(), db:errmsg(), query)
         active_quest_id = nil
      end

   elseif action == "timeout" then
      if active_quest_id then
         local now = os.time()
         local query = string.format(
            "UPDATE quests SET result = 'timeout', duration = %d - timestamp WHERE id = %d AND result IS NULL",
            now, active_quest_id)
         db:exec(query)
         dbcheck(db:errcode(), db:errmsg(), query)
         active_quest_id = nil
      end

   elseif action == "status" then
      -- Login/reconnect status check
      if data.status == "ready" then
         -- No active quest — close any open record
         close_open_quest()
      elseif data.targ and data.targ ~= "missing" and not active_quest_id then
         -- Active quest found but we have no record — create one
         local query = string.format(
            "INSERT INTO quests (timestamp, level, tier, remort, mob_name, area, room, timer, redo) " ..
            "VALUES (%d, %d, %s, %s, %s, %s, %s, %s, %s)",
            os.time(),
            cached_level or 0,
            cached_tier and tostring(cached_tier) or "NULL",
            cached_remorts and tostring(cached_remorts) or "NULL",
            fixsql(data.targ),
            fixsql(data.area),
            fixsql(data.room),
            data.timer and tostring(data.timer) or "NULL",
            cached_redos and tostring(cached_redos) or "NULL")
         db:exec(query)
         if not dbcheck(db:errcode(), db:errmsg(), query) then
            active_quest_id = db:last_insert_rowid()
         end
      end
   end
end

-- =============================================================================
-- Campaign: text trigger handlers
-- =============================================================================
function ldb_on_cp_start(name, line, wildcards)
   if not db then return end
   if not enabled then return end

   close_open_campaign()

   local query = string.format(
      "INSERT INTO campaigns (timestamp, level, tier, remort, redo) VALUES (%d, %d, %s, %s, %s)",
      os.time(),
      cached_level or 0,
      cached_tier and tostring(cached_tier) or "NULL",
      cached_remorts and tostring(cached_remorts) or "NULL",
      cached_redos and tostring(cached_redos) or "NULL")
   db:exec(query)
   if not dbcheck(db:errcode(), db:errmsg(), query) then
      active_campaign_id = db:last_insert_rowid()
   end

   send_silent_cp_info()
end

function ldb_on_cp_mob_killed(name, line, wildcards)
   -- Trigger kept for potential future use (e.g., kill counting).
end

function ldb_on_cp_complete(name, line, wildcards)
   if not db then return end
   if not active_campaign_id then return end

   -- Safety: disable cp info parsing if somehow still active
   disable_cpinfo_parsing()

   local now = os.time()
   db:exec(string.format(
      "UPDATE campaigns SET result = 'completed', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_campaign_id))
   dbcheck(db:errcode(), db:errmsg(), "cp_complete")

   -- Enable reward parsing
   EnableTrigger("trg_ldb_cp_reward", true)
   EnableTrigger("trg_ldb_cp_reward_end", true)
end

function ldb_on_cp_quit(name, line, wildcards)
   if not db then return end
   if not active_campaign_id then return end

   local now = os.time()
   local query = string.format(
      "UPDATE campaigns SET result = 'quit', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_campaign_id)
   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
   active_campaign_id = nil
end

-- =============================================================================
-- Campaign: silent cp info parsing handlers
-- =============================================================================
function ldb_on_cpinfo_none(name, line, wildcards)
   -- "You are not currently on a campaign."
   disable_cpinfo_parsing()
   close_open_campaign()
end

function ldb_on_cpinfo_level(name, line, wildcards)
   cpinfo_level = tonumber(wildcards[1])
end

function ldb_on_cpinfo_qp(name, line, wildcards)
   cpinfo_qp = tonumber(wildcards[1])
end

function ldb_on_cpinfo_gold(name, line, wildcards)
   local gold_str = wildcards[1]
   if gold_str then
      cpinfo_gold = tonumber((string.gsub(gold_str, ",", "")))
   end
end

function ldb_on_cpinfo_mob(name, line, wildcards)
   local mob_name = wildcards[1]
   local area = wildcards[2]
   if mob_name then
      mob_name = string.match(mob_name, "^%s*(.-)%s*$")  -- trim
   end
   if area then
      area = string.match(area, "^%s*(.-)%s*$")
   end
   table.insert(cpinfo_mob_buffer, {name = mob_name, area = area})
end

function ldb_on_cpinfo_end(name, line, wildcards)
   -- Closing separator: end of cp info output
   disable_cpinfo_parsing()

   if #cpinfo_mob_buffer == 0 then return end
   if not db or not enabled then return end

   -- Reconnect scenario: cp info shows an active campaign
   if not active_campaign_id then
      -- No saved campaign — create a new record from cp info data
      local level = cpinfo_level or cached_level or 0
      local query = string.format(
         "INSERT INTO campaigns (timestamp, level, tier, remort, redo) VALUES (%d, %d, %s, %s, %s)",
         os.time(),
         level,
         cached_tier and tostring(cached_tier) or "NULL",
         cached_remorts and tostring(cached_remorts) or "NULL",
         cached_redos and tostring(cached_redos) or "NULL")
      db:exec(query)
      if not dbcheck(db:errcode(), db:errmsg(), query) then
         active_campaign_id = db:last_insert_rowid()
      end
   else
      -- We have a saved campaign — check if it matches
      local saved_level = nil
      local saved_qp = nil
      for row in db:nrows(string.format(
         "SELECT level, expected_qp FROM campaigns WHERE id = %d", active_campaign_id)) do
         saved_level = row.level
         saved_qp = row.expected_qp
      end

      local match = (saved_level == cpinfo_level)
      if saved_qp and cpinfo_qp then
         match = match and (saved_qp == cpinfo_qp)
      end

      if not match then
         -- Different campaign — close old as unknown, create new
         close_open_campaign()
         local level = cpinfo_level or cached_level or 0
         local query = string.format(
            "INSERT INTO campaigns (timestamp, level, tier, remort, redo) VALUES (%d, %d, %s, %s, %s)",
            os.time(),
            level,
            cached_tier and tostring(cached_tier) or "NULL",
            cached_remorts and tostring(cached_remorts) or "NULL",
            cached_redos and tostring(cached_redos) or "NULL")
         db:exec(query)
         if not dbcheck(db:errcode(), db:errmsg(), query) then
            active_campaign_id = db:last_insert_rowid()
         end
      end
      -- If match, keep existing active_campaign_id — just refresh mob list
   end

   flush_cpinfo_mob_buffer()
end

function ldb_on_cpinfo_gag(name, line, wildcards)
   -- No-op handler for gagged cp info lines
end

-- =============================================================================
-- Campaign: reward parsing handlers
-- =============================================================================
function ldb_on_cp_reward(name, line, wildcards)
   if not active_campaign_id or not db then return end

   local amount_str = wildcards[1]
   local rtype = wildcards[2]
   if not amount_str or not rtype then return end

   local amount = tonumber((string.gsub(amount_str, ",", "")))
   if not amount then return end

   local field = nil
   if string.find(rtype, "quest point") then field = "qp"
   elseif string.find(rtype, "gold coin") then field = "gold"
   elseif string.find(rtype, "trivia point") then field = "tp"
   elseif string.find(rtype, "training session") then field = "trains"
   elseif string.find(rtype, "practice") then field = "pracs"
   end

   if field then
      db:exec(string.format(
         "UPDATE campaigns SET %s = COALESCE(%s, 0) + %d WHERE id = %d",
         field, field, amount, active_campaign_id))
      dbcheck(db:errcode(), db:errmsg(), "cp_reward " .. field)
   end
end

function ldb_on_cp_reward_end(name, line, wildcards)
   -- Closing "---" separator
   EnableTrigger("trg_ldb_cp_reward", false)
   EnableTrigger("trg_ldb_cp_reward_end", false)
   active_campaign_id = nil
end

-- =============================================================================
-- Campaign: delayed cp info check on plugin load (must be global for DoAfterSpecial)
-- =============================================================================
function delayed_cp_info()
   if not db then return end
   if not enabled then return end
   send_silent_cp_info()
end

-- =============================================================================
-- Global Quest: silent gq info/check parsing helpers
-- =============================================================================
local function enable_gqinfo_parsing()
   gqinfo_qp = nil
   gqinfo_gold = nil
   gqinfo_pracs = nil
   gqinfo_trains = nil
   gqinfo_tp = nil
   gqinfo_gq_number = nil
   gqinfo_status = nil
   EnableTrigger("trg_ldb_gqinfo_none", true)
   EnableTrigger("trg_ldb_gqinfo_norun", true)
   EnableTrigger("trg_ldb_gqinfo_multi", true)
   EnableTrigger("trg_ldb_gqinfo_qn", true)
   EnableTrigger("trg_ldb_gqinfo_status", true)
   EnableTrigger("trg_ldb_gqinfo_qp", true)
   EnableTrigger("trg_ldb_gqinfo_gold", true)
   EnableTrigger("trg_ldb_gqinfo_pracs", true)
   EnableTrigger("trg_ldb_gqinfo_trains", true)
   EnableTrigger("trg_ldb_gqinfo_tp", true)
   EnableTrigger("trg_ldb_gqinfo_gag", true)
   EnableTrigger("trg_ldb_gqinfo_end", true)
end

local function disable_gqinfo_parsing()
   EnableTrigger("trg_ldb_gqinfo_none", false)
   EnableTrigger("trg_ldb_gqinfo_norun", false)
   EnableTrigger("trg_ldb_gqinfo_multi", false)
   EnableTrigger("trg_ldb_gqinfo_qn", false)
   EnableTrigger("trg_ldb_gqinfo_status", false)
   EnableTrigger("trg_ldb_gqinfo_qp", false)
   EnableTrigger("trg_ldb_gqinfo_gold", false)
   EnableTrigger("trg_ldb_gqinfo_pracs", false)
   EnableTrigger("trg_ldb_gqinfo_trains", false)
   EnableTrigger("trg_ldb_gqinfo_tp", false)
   EnableTrigger("trg_ldb_gqinfo_gag", false)
   EnableTrigger("trg_ldb_gqinfo_end", false)
end

local gqcheck_generation = 0  -- generation counter to ignore stale timeout callbacks

local function enable_gqcheck_parsing()
   gqinfo_mob_buffer = {}
   gqcheck_generation = gqcheck_generation + 1
   EnableTrigger("trg_ldb_gqcheck_mob", true)
   EnableTrigger("trg_ldb_gqcheck_mob2", true)
   EnableTrigger("trg_ldb_gqcheck_none", true)
   EnableTrigger("trg_ldb_gqcheck_norun", true)
   EnableTrigger("trg_ldb_gqcheck_gag", true)
   -- Timer-based end detection (no closing separator in gq check output)
   -- Each mob line resets the timer by bumping generation and scheduling a new timeout
   DoAfterSpecial(1, "gqcheck_timeout(" .. gqcheck_generation .. ")", 12)
end

local function disable_gqcheck_parsing()
   gqcheck_generation = gqcheck_generation + 1  -- invalidate any pending timeout
   EnableTrigger("trg_ldb_gqcheck_mob", false)
   EnableTrigger("trg_ldb_gqcheck_mob2", false)
   EnableTrigger("trg_ldb_gqcheck_none", false)
   EnableTrigger("trg_ldb_gqcheck_norun", false)
   EnableTrigger("trg_ldb_gqcheck_gag", false)
end

local function send_silent_gq_info(gq_number)
   enable_gqinfo_parsing()
   if gq_number then
      SendNoEcho("gq info " .. gq_number)
   else
      SendNoEcho("gq info")
   end
end

local function flush_gqcheck_mob_buffer()
   if not active_gquest_id or not db then return end
   if #gqinfo_mob_buffer == 0 then return end

   -- Delete old mobs for this gquest and re-insert
   db:exec(string.format("DELETE FROM gquest_mobs WHERE gquest_id = %d", active_gquest_id))

   for _, mob in ipairs(gqinfo_mob_buffer) do
      local query = string.format(
         "INSERT INTO gquest_mobs (gquest_id, mob_name, area, mob_count) VALUES (%d, %s, %s, %d)",
         active_gquest_id, fixsql(mob.name), fixsql(mob.area), mob.count or 1)
      db:exec(query)
      dbcheck(db:errcode(), db:errmsg(), query)
   end

   -- Update mob_count and expected rewards on gquest
   local query = string.format(
      "UPDATE gquests SET mob_count = (SELECT COUNT(*) FROM gquest_mobs WHERE gquest_id = %d), " ..
      "expected_qp = %s, expected_gold = %s WHERE id = %d",
      active_gquest_id,
      gqinfo_qp and tostring(gqinfo_qp) or "NULL",
      gqinfo_gold and tostring(gqinfo_gold) or "NULL",
      active_gquest_id)
   db:exec(query)
   dbcheck(db:errcode(), db:errmsg(), query)
end

-- =============================================================================
-- Global Quest: text trigger handlers
-- =============================================================================
function ldb_on_gq_join(name, line, wildcards)
   if not db then return end
   if not enabled then return end

   local gq_num = tonumber(wildcards[1])

   close_open_gquest()

   local query = string.format(
      "INSERT INTO gquests (timestamp, gq_number, level, tier, remort, redo) " ..
      "VALUES (%d, %s, %d, %s, %s, %s)",
      os.time(),
      gq_num and tostring(gq_num) or "NULL",
      cached_level or 0,
      cached_tier and tostring(cached_tier) or "NULL",
      cached_remorts and tostring(cached_remorts) or "NULL",
      cached_redos and tostring(cached_redos) or "NULL")
   db:exec(query)
   if not dbcheck(db:errcode(), db:errmsg(), query) then
      active_gquest_id = db:last_insert_rowid()
   end

   -- Fetch gq info (rewards), then chain gq check (mob list) from gqinfo_end
   send_silent_gq_info(gq_num)
end

function ldb_on_gq_mob_killed(name, line, wildcards)
   if not active_gquest_id then return end
   -- Enable per-mob QP capture for 1 second
   EnableTrigger("trg_ldb_gq_mob_qp", true)
   DoAfterSpecial(1, 'EnableTrigger("trg_ldb_gq_mob_qp", false)', 12)
end

function ldb_on_gq_mob_qp(name, line, wildcards)
   if not active_gquest_id or not db then return end
   local amount = tonumber(wildcards[1])
   if not amount then return end
   db:exec(string.format(
      "UPDATE gquests SET qp = COALESCE(qp, 0) + %d WHERE id = %d",
      amount, active_gquest_id))
end

function ldb_on_gq_won(name, line, wildcards)
   if not db then return end
   if not active_gquest_id then return end

   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'won', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "gq_won")

   -- Enable reward parsing for 1 second
   EnableTrigger("trg_ldb_gq_reward", true)
   DoAfterSpecial(1, 'EnableTrigger("trg_ldb_gq_reward", false); active_gquest_id = nil', 12)
end

function ldb_on_gq_finished(name, line, wildcards)
   if not db then return end
   if not active_gquest_id then return end

   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'completed', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "gq_finished")

   -- Enable reward parsing for 1 second (may or may not fire)
   EnableTrigger("trg_ldb_gq_reward", true)
   DoAfterSpecial(1, 'EnableTrigger("trg_ldb_gq_reward", false); active_gquest_id = nil', 12)
end

function ldb_on_gq_ended(name, line, wildcards)
   if not db then return end
   if not active_gquest_id then return end

   -- Only act if gq_number matches our active gquest
   local gq_num = tonumber(wildcards[1])
   if gq_num then
      local saved_gq_num = nil
      for row in db:nrows(string.format(
         "SELECT gq_number FROM gquests WHERE id = %d", active_gquest_id)) do
         saved_gq_num = row.gq_number
      end
      if saved_gq_num and saved_gq_num ~= gq_num then return end
   end

   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'expired', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "gq_ended")
   active_gquest_id = nil
end

function ldb_on_gq_cancelled(name, line, wildcards)
   if not db then return end
   if not active_gquest_id then return end

   local gq_num = tonumber(wildcards[1])
   if gq_num then
      local saved_gq_num = nil
      for row in db:nrows(string.format(
         "SELECT gq_number FROM gquests WHERE id = %d", active_gquest_id)) do
         saved_gq_num = row.gq_number
      end
      if saved_gq_num and saved_gq_num ~= gq_num then return end
   end

   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'cancelled', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "gq_cancelled")
   active_gquest_id = nil
end

function ldb_on_gq_quit(name, line, wildcards)
   if not db then return end
   if not active_gquest_id then return end

   local now = os.time()
   db:exec(string.format(
      "UPDATE gquests SET result = 'quit', duration = %d - timestamp WHERE id = %d AND result IS NULL",
      now, active_gquest_id))
   dbcheck(db:errcode(), db:errmsg(), "gq_quit")
   active_gquest_id = nil
end

function ldb_on_gq_not_in(name, line, wildcards)
   -- Only close if we think we're in a gquest
   if active_gquest_id then
      close_open_gquest()
   end
end

-- =============================================================================
-- Global Quest: reward parsing handler
-- =============================================================================
function ldb_on_gq_reward(name, line, wildcards)
   if not active_gquest_id or not db then return end

   local amount_str = wildcards[1]
   local rtype = wildcards[2]
   if not amount_str or not rtype then return end

   local amount = tonumber((string.gsub(amount_str, ",", "")))
   if not amount then return end

   local field = nil
   if string.find(rtype, "quest point") then field = "qp"
   elseif string.find(rtype, "gold coin") then field = "gold"
   elseif string.find(rtype, "trivia point") then field = "tp"
   elseif string.find(rtype, "training session") then field = "trains"
   elseif string.find(rtype, "practice") then field = "pracs"
   end

   if field then
      db:exec(string.format(
         "UPDATE gquests SET %s = COALESCE(%s, 0) + %d WHERE id = %d",
         field, field, amount, active_gquest_id))
      dbcheck(db:errcode(), db:errmsg(), "gq_reward " .. field)
   end
end

-- =============================================================================
-- Global Quest: silent gq info parsing handlers
-- =============================================================================
function ldb_on_gqinfo_none(name, line, wildcards)
   disable_gqinfo_parsing()
   close_open_gquest()
end

function ldb_on_gqinfo_multi(name, line, wildcards)
   -- "There is more than one global quest running." has a follow-up line
   -- "Use syntax: 'gquest info [num]'..." that needs to be gagged too.
   -- Delay disable by 1s so the gag trigger catches the follow-up line.
   DoAfterSpecial(1, "ldb_on_gqinfo_multi_cleanup()", 12)
end

function ldb_on_gqinfo_multi_cleanup()
   disable_gqinfo_parsing()
   -- Don't close active gquest — "more than one" doesn't mean we're not in one.
   -- Fall back to gq check to get mob list if we have an active gquest.
   if active_gquest_id then
      enable_gqcheck_parsing()
      SendNoEcho("gq check")
   end
end

function ldb_on_gqinfo_qn(name, line, wildcards)
   gqinfo_gq_number = tonumber(wildcards[1])
end

function ldb_on_gqinfo_status(name, line, wildcards)
   gqinfo_status = wildcards[1]
end

function ldb_on_gqinfo_qp(name, line, wildcards)
   gqinfo_qp = tonumber(wildcards[1])
end

function ldb_on_gqinfo_gold(name, line, wildcards)
   local gold_str = wildcards[1]
   if gold_str then
      gqinfo_gold = tonumber((string.gsub(gold_str, ",", "")))
   end
end

function ldb_on_gqinfo_pracs(name, line, wildcards)
   gqinfo_pracs = tonumber(wildcards[1])
end

function ldb_on_gqinfo_trains(name, line, wildcards)
   gqinfo_trains = tonumber(wildcards[1])
end

function ldb_on_gqinfo_tp(name, line, wildcards)
   gqinfo_tp = tonumber(wildcards[1])
end

function ldb_on_gqinfo_gag(name, line, wildcards)
   -- No-op handler for gagged gq info lines
end

function ldb_on_gqinfo_end(name, line, wildcards)
   -- Closing separator: end of gq info output
   disable_gqinfo_parsing()

   -- Guard: if no gq info data was parsed, this is a stray separator (e.g.,
   -- from campaign reward output). Ignore it.
   if not gqinfo_gq_number and not gqinfo_qp and not gqinfo_status then return end

   -- If gquest is Finished, don't parse mob list
   if gqinfo_status == "Finished" then
      close_open_gquest()
      return
   end

   if not db or not enabled then return end

   -- Only update existing records — never create new ones here.
   -- The join trigger is the sole creator of GQ records.
   if not active_gquest_id then return end

   if gqinfo_gq_number then
      -- Verify gq_number matches saved record (check before updating)
      local saved_gq_num = nil
      for row in db:nrows(string.format(
         "SELECT gq_number FROM gquests WHERE id = %d", active_gquest_id)) do
         saved_gq_num = row.gq_number
      end
      if saved_gq_num and saved_gq_num ~= gqinfo_gq_number then
         -- gq info returned a different GQ than ours — we're no longer in our GQ
         close_open_gquest()
         return
      end
      -- Update gq_number on existing record (in case it was NULL from join)
      db:exec(string.format(
         "UPDATE gquests SET gq_number = %d WHERE id = %d",
         gqinfo_gq_number, active_gquest_id))
   end

   -- Chain: send gq check to fetch mob list
   if active_gquest_id then
      enable_gqcheck_parsing()
      SendNoEcho("gq check")
   end
end

-- =============================================================================
-- Global Quest: silent gq check parsing handlers
-- =============================================================================
function ldb_on_gqcheck_mob(name, line, wildcards)
   local count = tonumber(wildcards[1]) or 1
   local mob_name = wildcards[2]
   local area = wildcards[3]
   if mob_name then
      mob_name = string.match(mob_name, "^%s*(.-)%s*$")  -- trim
   end
   if area then
      area = string.match(area, "^%s*(.-)%s*$")
   end
   table.insert(gqinfo_mob_buffer, {name = mob_name, area = area, count = count})
   -- Reset timeout: bump generation so old timer becomes stale, schedule new one
   gqcheck_generation = gqcheck_generation + 1
   DoAfterSpecial(1, "gqcheck_timeout(" .. gqcheck_generation .. ")", 12)
end

-- Format 2: "You still have to kill N * mob (area)" (after gquest starts)
function ldb_on_gqcheck_mob2(name, line, wildcards)
   local count = tonumber(wildcards[1]) or 1
   local mob_name = wildcards[2]
   local area = wildcards[3]
   if mob_name then
      mob_name = string.match(mob_name, "^%s*(.-)%s*$")
   end
   if area then
      area = string.match(area, "^%s*(.-)%s*$")
   end
   table.insert(gqinfo_mob_buffer, {name = mob_name, area = area, count = count})
   -- Reset timeout (same as format 1)
   gqcheck_generation = gqcheck_generation + 1
   DoAfterSpecial(1, "gqcheck_timeout(" .. gqcheck_generation .. ")", 12)
end

function ldb_on_gqcheck_none(name, line, wildcards)
   -- Don't disable parsing here — let the 1s timeout handle it so the gag
   -- trigger catches follow-up lines ("You may win...", "You can be rewarded...").
   -- The timeout's flush_gqcheck_mob_buffer() is a no-op since gquest is closed.
   close_open_gquest()
end

function ldb_on_gqcheck_gag(name, line, wildcards)
   -- No-op handler for gagged gq check lines
end

-- Timer-based end detection for gq check (must be global for DoAfterSpecial)
-- Generation parameter prevents stale timeouts from acting after timer reset
function gqcheck_timeout(gen)
   if gen ~= gqcheck_generation then return end  -- stale timeout, ignore
   disable_gqcheck_parsing()
   flush_gqcheck_mob_buffer()
end

-- =============================================================================
-- Global Quest: delayed gq info check on plugin load (must be global)
-- =============================================================================
function delayed_gq_info()
   if not db then return end
   if not enabled then return end
   -- Only reconcile if we have a saved active gquest from a previous session.
   -- The join trigger is the sole creator of GQ records; we never discover GQs
   -- from gq info alone (it shows details even when not joined).
   if not active_gquest_id then return end

   -- Look up gq_number for targeted gq info (avoids "more than one" ambiguity)
   local gq_number = nil
   for row in db:nrows(string.format(
      "SELECT gq_number FROM gquests WHERE id = %d", active_gquest_id)) do
      gq_number = row.gq_number
   end

   if gq_number then
      send_silent_gq_info(gq_number)
   else
      -- No gq_number saved — use gq check to verify we're still in a GQ
      enable_gqcheck_parsing()
      SendNoEcho("gq check")
   end
end

-- =============================================================================
-- Helper: format number with commas
-- =============================================================================
local function format_number(n)
   local formatted = tostring(n)
   while true do
      local k
      formatted, k = string.gsub(formatted, "^(-?%d+)(%d%d%d)", "%1,%2")
      if k == 0 then break end
   end
   return formatted
end

-- =============================================================================
-- Helper: format number abbreviated (1234 -> "1.2k", 1234567 -> "1.2M")
-- =============================================================================
local function format_abbreviated(n)
   if not n or n == 0 then return "0" end
   local abs_n = math.abs(n)
   local sign = n < 0 and "-" or ""
   if abs_n >= 1000000 then
      return sign .. string.format("%.1fM", abs_n / 1000000)
   elseif abs_n >= 1000 then
      return sign .. string.format("%.1fk", abs_n / 1000)
   else
      return sign .. tostring(math.floor(abs_n))
   end
end

-- =============================================================================
-- Helper: format time in seconds to human-readable
-- =============================================================================
local function format_time(seconds)
   if not seconds then return "N/A" end
   if seconds < 0 then return "N/A" end
   if seconds < 60 then
      return string.format("%.1fs", seconds)
   elseif seconds < 3600 then
      local m = math.floor(seconds / 60)
      local s = seconds - m * 60
      return string.format("%dm %ds", m, s)
   else
      local h = math.floor(seconds / 3600)
      local m = math.floor((seconds - h * 3600) / 60)
      return string.format("%dh %dm", h, m)
   end
end

-- =============================================================================
-- Helper: bracket label and SQL CASE expression for level brackets
-- =============================================================================
local BRACKETS = {
   {label = "1-50",    min = 1,   max = 50},
   {label = "51-100",  min = 51,  max = 100},
   {label = "101-150", min = 101, max = 150},
   {label = "151-200", min = 151, max = 200},
}

-- =============================================================================
-- Helper: check DB is available for queries
-- =============================================================================
local function require_db()
   if not db then
      ColourNote("red", "", PREFIX .. "No database open. Try reloading the plugin.")
      return false
   end
   return true
end

-- =============================================================================
-- Helper: format tier label with redo (hides +0)
-- =============================================================================
local function format_tier_label(tier, redo)
   if redo and redo > 0 then
      return string.format("T%d+%d", tier, redo)
   else
      return string.format("T%d", tier)
   end
end

-- =============================================================================
-- Helper: SQL WHERE fragment for redo (handles NULL as 0)
-- =============================================================================
local function redo_where(redo_value)
   return string.format("COALESCE(redo, 0) = %d", redo_value or 0)
end

-- =============================================================================
-- Helper: parse tier/remort/redo filter from command arguments
-- Returns: {all=true} or {tier=N, remort=N} or {tier=N} or {remort=N} or {}
-- Returns nil on invalid input (error already printed)
-- =============================================================================
local function parse_args(str, opts)
   opts = opts or {}
   if not str or str == "" then return {} end
   local s = string.lower(string.match(str, "^%s*(.-)%s*$"))
   if s == "" then return {} end
   if s == "all" then return {all = true} end

   local result = {}

   -- Bare number: context-dependent (remort by default, tier if opts.bare_is_tier)
   local bare = string.match(s, "^(%d+)$")
   if bare then
      if opts.bare_is_tier then
         result.tier = tonumber(bare)
         result.redo = 0
      else
         result.remort = tonumber(bare)
      end
      return result
   end

   -- Parse T<n> with optional +<redo>
   local t, plus_redo = string.match(s, "t(%d+)%+(%d+)")
   if not t then
      t = string.match(s, "t(%d+)")
   end
   local r = string.match(s, "r(%d+)")

   if not t and not r then
      ColourNote("red", "", PREFIX .. "Invalid filter: " .. str)
      ColourNote("red", "", PREFIX .. "Use: all, T1 R5, T9+3, T9+3 R5, R4, or T1")
      return nil
   end

   if t then
      result.tier = tonumber(t)
      result.redo = plus_redo and tonumber(plus_redo) or 0
   end
   if r then result.remort = tonumber(r) end

   return result
end

-- =============================================================================
-- Command: ldb (status)
-- =============================================================================
function ldb_status(name, line, wildcards)
   ColourNote("cyan", "", PREFIX .. "Status")
   ColourNote("cyan", "", "  Enabled: " .. (enabled and "ON" or "OFF"))
   ColourNote("cyan", "", "  Database: " .. (db_path or "(none)"))

   if db then
      local kill_count = 0
      local death_count = 0
      for row in db:nrows("SELECT COUNT(*) as cnt FROM kills") do
         kill_count = row.cnt
      end
      for row in db:nrows("SELECT COUNT(*) as cnt FROM deaths") do
         death_count = row.cnt
      end
      ColourNote("cyan", "", "  Kills recorded: " .. format_number(kill_count))
      ColourNote("cyan", "", "  Deaths recorded: " .. format_number(death_count))

      local quest_count = 0
      local campaign_count = 0
      for row in db:nrows("SELECT COUNT(*) as cnt FROM quests") do
         quest_count = row.cnt
      end
      for row in db:nrows("SELECT COUNT(*) as cnt FROM campaigns") do
         campaign_count = row.cnt
      end
      ColourNote("cyan", "", "  Quests recorded: " .. format_number(quest_count))
      ColourNote("cyan", "", "  Campaigns recorded: " .. format_number(campaign_count))

      local pup_event_count = 0
      for row in db:nrows("SELECT COUNT(*) as cnt FROM pup_events") do
         pup_event_count = row.cnt
      end
      ColourNote("cyan", "", "  Pup events recorded: " .. format_number(pup_event_count))

      local gquest_count = 0
      for row in db:nrows("SELECT COUNT(*) as cnt FROM gquests") do
         gquest_count = row.cnt
      end
      ColourNote("cyan", "", "  GQuests recorded: " .. format_number(gquest_count))
   end

   if cached_level then
      ColourNote("cyan", "", "  Current level: " .. cached_level)
      if cached_redos and cached_redos > 0 then
         ColourNote("cyan", "", "  Current redo: " .. cached_redos)
      end
      if cached_level >= 200 and cached_pups then
         ColourNote("cyan", "", "  Current powerup: " .. cached_pups)
         if cached_tnl and cached_perlevel then
            ColourNote("cyan", "", "  TNL: " .. cached_tnl .. " / " .. cached_perlevel)
         end
      end
   end
   if cached_zone then
      ColourNote("cyan", "", "  Current zone: " .. cached_zone)
   end
   if in_combat and fight_start then
      ColourNote("cyan", "", "  In combat: " .. fight_start.enemy ..
         " (round " .. round_count .. ", dmg " .. format_number(damage_total) .. ")")
   end
end

-- =============================================================================
-- Command: ldb help
-- =============================================================================
function ldb_help(name, line, wildcards)
   ColourNote("cyan", "", PREFIX .. "Commands:")
   ColourNote("cyan", "", "  ldb                     - Show status")
   ColourNote("cyan", "", "  ldb help                - Show this help")
   ColourNote("cyan", "", "  ldb on / off            - Enable/disable data collection")
   ColourNote("cyan", "", "  ldb level [N] [filter]  - Kill breakdown for level N (default: current)")
   ColourNote("cyan", "", "  ldb this [filter]       - Kill breakdown for current level")
   ColourNote("cyan", "", "  ldb last [filter]       - Kill breakdown for previous level")
   ColourNote("cyan", "", "  ldb pup [filter|zone]   - Powerup productivity stats")
   ColourNote("cyan", "", "  ldb pup <id>            - Per-kill detail for a specific powerup")
   ColourNote("cyan", "", "  ldb pup list [N]        - Last N powerup events (default 10)")
   ColourNote("cyan", "", "  ldb zone [name]         - Stats for a zone (default: current)")
   ColourNote("cyan", "", "  ldb mob <name>          - Stats for a mob (substring match)")
   ColourNote("cyan", "", "  ldb top mobs [N]        - Top N mobs by kill count (default 10)")
   ColourNote("cyan", "", "  ldb top zones [N]       - Top N zones by total XP (default 10)")
   ColourNote("cyan", "", "  ldb top xp [N]          - Top N mobs by avg XP per kill (default 10)")
   ColourNote("cyan", "", "  ldb remort [R] [T<n>]   - Bracket summary for a remort")
   ColourNote("cyan", "", "                            Default: current tier & remort")
   ColourNote("cyan", "", "                            ldb remort 4 = R4 current tier")
   ColourNote("cyan", "", "                            ldb remort T1 R4 = T1 R4")
   ColourNote("cyan", "", "  ldb tier [T]             - Compare remorts within a tier")
   ColourNote("cyan", "", "                            Default: current tier")
   ColourNote("cyan", "", "                            ldb tier 1 = T1")
   ColourNote("cyan", "", "  ldb quest [filter]      - Quest log (default: current tier & remort)")
   ColourNote("cyan", "", "  ldb cp [filter]         - Campaign log (default: current tier & remort)")
   ColourNote("cyan", "", "  ldb cp show <id>        - Detailed view of a specific campaign")
   ColourNote("cyan", "", "  ldb gq [filter]         - Global quest log (default: current tier & remort)")
   ColourNote("cyan", "", "  ldb gq show <id>        - Detailed view of a specific global quest")
   ColourNote("cyan", "", "  ldb deaths [N]          - Last N deaths (default 10)")
   ColourNote("cyan", "", "  ldb db                  - Database file info")
   ColourNote("cyan", "", "")
   ColourNote("cyan", "", "  Filters: default = current tier, redo & remort")
   ColourNote("cyan", "", "    all                   - All tiers and remorts (separate sections)")
   ColourNote("cyan", "", "    T1 R5                 - Specific tier and remort")
   ColourNote("cyan", "", "    T9+3 R5               - Specific tier+redo and remort")
   ColourNote("cyan", "", "    T1                    - All remorts within a tier (separate sections)")
   ColourNote("cyan", "", "    R4                    - Specific remort, current tier")
end

-- =============================================================================
-- Command: ldb on / ldb off
-- =============================================================================
function ldb_on(name, line, wildcards)
   enabled = true
   if not db then open_db() end
   ColourNote("cyan", "", PREFIX .. "Data collection enabled.")
end

function ldb_off(name, line, wildcards)
   enabled = false
   ColourNote("cyan", "", PREFIX .. "Data collection disabled.")
end

-- =============================================================================
-- Command: ldb level [N]
-- =============================================================================
function ldb_level(name, line, wildcards)
   if not require_db() then return end
   local arg = tonumber(wildcards[1])
   local filter = parse_args(wildcards[2])
   if not filter then return end

   if not arg and cached_level and cached_level >= 200 then
      ColourNote("cyan", "", PREFIX .. "Use 'ldb pup' for powerup stats.")
      return
   end

   local level = arg or cached_level
   if not level then
      ColourNote("red", "", PREFIX .. "Current level unknown. Try again after GMCP data arrives.")
      return
   end
   show_level_stats(level, nil, filter)
end

-- =============================================================================
-- Command: ldb thislevel
-- =============================================================================
function ldb_thislevel(name, line, wildcards)
   if not require_db() then return end
   local filter = parse_args(wildcards[1])
   if not filter then return end
   if not cached_level then
      ColourNote("red", "", PREFIX .. "Current level unknown. Try again after GMCP data arrives.")
      return
   end
   if cached_level >= 200 then
      ColourNote("cyan", "", PREFIX .. "Use 'ldb pup' for powerup stats.")
      return
   end
   show_level_stats(cached_level, nil, filter)
end

-- =============================================================================
-- Command: ldb lastlevel
-- =============================================================================
function ldb_lastlevel(name, line, wildcards)
   if not require_db() then return end
   local filter = parse_args(wildcards[1])
   if not filter then return end
   if not cached_level then
      ColourNote("red", "", PREFIX .. "Current level unknown. Try again after GMCP data arrives.")
      return
   end
   if cached_level >= 200 then
      ColourNote("cyan", "", PREFIX .. "Use 'ldb pup' for powerup stats.")
      return
   end
   if cached_level <= 1 then
      ColourNote("yellow", "", PREFIX .. "No previous level.")
      return
   end
   show_level_stats(cached_level - 1, nil, filter)
end

-- =============================================================================
-- Command: ldb pup [filter|id|zone]
-- =============================================================================

-- Helper: build pup WHERE clause from filter + tier/remort defaults
local function pup_where(filter)
   local tier = filter.tier or cached_tier
   local redo = filter.redo
   if not filter.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end
   local remort = filter.remort or cached_remorts
   if filter.all then
      return "level >= 200", nil, nil, nil
   end
   if not tier or not remort then return nil end
   if filter.tier and not filter.remort then
      -- Tier only: all remorts within tier
      return string.format("level >= 200 AND tier = %d AND %s", tier, redo_where(redo)), tier, nil, redo
   end
   return string.format("level >= 200 AND tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo)), tier, remort, redo
end

-- Helper: render per-area pup productivity table
local function render_pup_area_table(kill_where, avg_trains, xp_per_level)
   if not xp_per_level or xp_per_level <= 0 then xp_per_level = cached_perlevel or 1000 end

   local area_query = string.format(
      "SELECT zone, COUNT(*) as kills, AVG(xp_gained) as avg_xp, " ..
      "AVG(combat_time) as avg_time " ..
      "FROM kills WHERE %s " ..
      "AND (mob_level IS NULL OR mob_level > 200) " ..
      "GROUP BY zone ORDER BY kills DESC",
      kill_where)

   local areas = {}
   for row in db:nrows(area_query) do
      table.insert(areas, row)
   end

   if #areas == 0 then
      ColourNote("yellow", "", "  No pup kills recorded.")
      return
   end

   local fmt = "  %-16s  %5s  %5s  %7s  %7s  %6s"
   ColourNote("cyan", "", string.format(fmt,
      "Area", "Kills", "AvgXP", "AvgTime", "Est/Pup", "Trn/Hr"))
   ColourNote("cyan", "", string.format(fmt,
      "----------------", "-----", "-----", "-------", "-------", "------"))

   for _, a in ipairs(areas) do
      local avg_time_str = "N/A"
      local est_pup_str = "N/A"
      local trn_hr_str = "N/A"

      if a.avg_time and a.avg_time > 0 then
         avg_time_str = format_time(a.avg_time)
      end

      if a.avg_xp and a.avg_xp > 0 and a.avg_time and a.avg_time > 0 then
         local est_time_per_pup = (xp_per_level / a.avg_xp) * a.avg_time
         est_pup_str = format_time(est_time_per_pup)
         if avg_trains and est_time_per_pup > 0 then
            trn_hr_str = tostring(math.floor(avg_trains / est_time_per_pup * 3600))
         end
      end

      local zone_display = a.zone
      if #zone_display > 16 then zone_display = string.sub(zone_display, 1, 15) .. "+" end

      ColourNote("white", "", string.format(fmt,
         zone_display,
         format_abbreviated(a.kills),
         format_abbreviated(math.floor(a.avg_xp)),
         avg_time_str,
         est_pup_str,
         trn_hr_str))
   end
end

-- Helper: query and render pup summary + area table for a given pup/kill WHERE clause
-- Returns pup_count, avg_trains, avg_xp_per_level for further use
local function render_pup_section(pup_where_clause, kill_where)
   local pup_count = 0
   local total_trains = 0
   local avg_trains = nil
   local avg_xp_per_level = nil

   for row in db:nrows(string.format(
      "SELECT COUNT(*) as cnt, COALESCE(SUM(trains_earned), 0) as trains, " ..
      "AVG(trains_earned) as avg_t, " ..
      "AVG(xp_per_level) as avg_xpl " ..
      "FROM pup_events WHERE %s", pup_where_clause)) do
      pup_count = row.cnt
      total_trains = row.trains
      avg_trains = row.avg_t
      avg_xp_per_level = row.avg_xpl
   end

   -- Check for pup kills (may exist without pup_events for historical data)
   local pup_kills = 0
   for row in db:nrows(string.format(
      "SELECT COUNT(*) as cnt FROM kills WHERE %s " ..
      "AND (mob_level IS NULL OR mob_level > 200)", kill_where)) do
      pup_kills = row.cnt
   end

   if pup_count == 0 and pup_kills == 0 then
      return pup_count, avg_trains, avg_xp_per_level
   end

   Note("")

   if pup_count > 0 then
      local avg_t_str = avg_trains and string.format("%.1f", avg_trains) or "N/A"
      ColourNote("white", "", string.format(
         "  Powerups: %d     Trains: %d (%s/pup)",
         pup_count, total_trains, avg_t_str))

      local total_combat_time = 0
      for row in db:nrows(string.format(
         "SELECT COALESCE(SUM(combat_time), 0) as t FROM kills WHERE %s " ..
         "AND combat_time IS NOT NULL AND (mob_level IS NULL OR mob_level > 200)",
         kill_where)) do
         total_combat_time = row.t
      end
      local avg_time_per_pup = pup_count > 0 and total_combat_time / pup_count or 0
      ColourNote("white", "", string.format(
         "  Combat time: %s (%s/pup)",
         format_time(total_combat_time),
         format_time(avg_time_per_pup)))
   else
      ColourNote("yellow", "", "  Pup kills: " .. pup_kills ..
         " (no pup events recorded; tracking started in v7.0)")
   end

   if pup_kills > 0 then
      Note("")
      local xp_per_level = avg_xp_per_level or cached_perlevel or 1000
      render_pup_area_table(kill_where, avg_trains, xp_per_level)
   end

   return pup_count, avg_trains, avg_xp_per_level
end

-- Helper: render a list of pup event rows
local function render_pup_list(pup_where_clause, limit)
   local pups = {}
   for row in db:nrows(string.format(
      "SELECT id, timestamp, tier, remort, redo, pup_number, zone, trains_earned " ..
      "FROM (SELECT * FROM pup_events WHERE %s ORDER BY id DESC LIMIT %d) ORDER BY id ASC",
      pup_where_clause, limit)) do
      table.insert(pups, row)
   end

   if #pups == 0 then return end

   local fmt = "  %5s  %-16s  %-7s  %5s  %-16s  %6s"
   ColourNote("cyan", "", string.format(fmt, "ID", "When", "T/R", "Pup#", "Zone", "Trains"))
   ColourNote("cyan", "", string.format(fmt, "-----", "----------------", "-------", "-----", "----------------", "------"))

   for _, p in ipairs(pups) do
      local tr_label = format_tier_label(p.tier, p.redo) .. "R" .. p.remort
      local zone_display = p.zone or "?"
      if #zone_display > 16 then zone_display = string.sub(zone_display, 1, 15) .. "+" end
      local trains_str = p.trains_earned and tostring(p.trains_earned) or "N/A"
      local when_str = os.date("%Y-%m-%d %H:%M", p.timestamp)
      ColourNote("white", "", string.format(fmt,
         tostring(p.id), when_str, tr_label, tostring(p.pup_number),
         zone_display, trains_str))
   end
end

-- Show pup overview for a filter (summary + per-area table)
local function show_pup_overview(filter)
   local kill_where, tier, remort, redo = pup_where(filter)
   if not kill_where then
      ColourNote("red", "", PREFIX .. "Current tier/remort unknown. Try again after GMCP data arrives.")
      return
   end

   -- Build label
   local label
   if filter.all then
      label = "all"
   elseif tier and not remort then
      label = format_tier_label(tier, redo)
   else
      label = format_tier_label(tier or 0, redo) .. " R" .. (remort or 0)
   end

   -- Build pup_events WHERE clause matching the kill_where scope
   local pup_where_clause
   if filter.all then
      pup_where_clause = "1=1"
   elseif tier and not remort then
      pup_where_clause = string.format("tier = %d AND %s", tier, redo_where(redo))
   else
      pup_where_clause = string.format("tier = %d AND remort = %d AND %s", tier or 0, remort or 0, redo_where(redo))
   end

   ColourNote("cyan", "", PREFIX .. "Powerup Summary (" .. label .. "):")
   render_pup_section(pup_where_clause, kill_where)

   -- Last 5 pup events
   Note("")
   ColourNote("cyan", "", "  Recent powerups:")
   render_pup_list(pup_where_clause, 5)
end

-- Show per-kill detail for a specific pup event by ID
local function show_pup_detail(pup_id)
   -- Look up this pup event
   local pup_event = nil
   for row in db:nrows(string.format(
      "SELECT * FROM pup_events WHERE id = %d", pup_id)) do
      pup_event = row
   end

   if not pup_event then
      ColourNote("red", "", PREFIX .. "No pup event found with ID " .. pup_id .. ".")
      return
   end

   local pup_redo = pup_event.redo or 0

   -- Find previous pup event for same tier+remort+redo to get time bounds
   local prev_timestamp = 0
   for row in db:nrows(string.format(
      "SELECT MAX(timestamp) as ts FROM pup_events " ..
      "WHERE tier = %d AND remort = %d AND %s AND id < %d",
      pup_event.tier, pup_event.remort, redo_where(pup_redo), pup_id)) do
      if row.ts then prev_timestamp = row.ts end
   end

   -- Query kills between prev pup and this pup
   local kills_query = string.format(
      "SELECT mob_name, zone, xp_gained, damage_total, rounds, mob_level, combat_time " ..
      "FROM kills WHERE level >= 200 AND tier = %d AND remort = %d AND %s " ..
      "AND timestamp > %d AND timestamp <= %d " ..
      "AND (mob_level IS NULL OR mob_level > 200) " ..
      "ORDER BY timestamp",
      pup_event.tier, pup_event.remort, redo_where(pup_redo), prev_timestamp, pup_event.timestamp)

   local kills = {}
   for row in db:nrows(kills_query) do
      table.insert(kills, row)
   end

   -- Header
   local label = format_tier_label(pup_event.tier, pup_redo) .. string.format(" R%d, pup %d", pup_event.remort, pup_event.pup_number)
   local zone_str = pup_event.zone and (" - " .. pup_event.zone) or ""
   ColourNote("cyan", "", string.format("%sPowerup #%d (%s)%s:", PREFIX, pup_id, label, zone_str))

   if #kills == 0 then
      ColourNote("yellow", "", "  No kills found for this powerup.")
   else
      -- Calculate column widths
      local max_mob = 3
      local max_zone = 4
      for _, k in ipairs(kills) do
         if #k.mob_name > max_mob then max_mob = #k.mob_name end
         if #k.zone > max_zone then max_zone = #k.zone end
      end
      if max_mob > 24 then max_mob = 24 end
      if max_zone > 16 then max_zone = 16 end

      local num_w = #tostring(#kills)
      if num_w < 1 then num_w = 1 end

      local function trunc(s, w)
         if #s > w then return string.sub(s, 1, w - 1) .. "+" else return s end
      end

      local hdr = string.format(
         "  %-" .. num_w .. "s  %-" .. max_mob .. "s  %-" .. max_zone .. "s  %7s  %5s  %3s  %4s  %5s",
         "#", "Mob", "Zone", "XP", "Dmg", "Rnd", "MLvl", "Time")
      ColourNote("cyan", "", hdr)
      local sep = "  " .. string.rep("-", num_w) .. "  " .. string.rep("-", max_mob) .. "  " ..
         string.rep("-", max_zone) .. "  " .. string.rep("-", 7) .. "  " ..
         string.rep("-", 5) .. "  " .. string.rep("-", 3) .. "  " .. string.rep("-", 4) ..
         "  " .. string.rep("-", 5)
      ColourNote("cyan", "", sep)

      local total_xp = 0
      local total_damage = 0
      local total_rounds = 0
      local total_time = 0
      local time_count = 0

      for i, k in ipairs(kills) do
         total_xp = total_xp + k.xp_gained
         total_damage = total_damage + k.damage_total
         total_rounds = total_rounds + k.rounds
         if k.combat_time then
            total_time = total_time + k.combat_time
            time_count = time_count + 1
         end

         local mlvl_str = k.mob_level and tostring(k.mob_level) or "-"
         local time_str = k.combat_time and format_time(k.combat_time) or "N/A"
         ColourNote("white", "", string.format(
            "  %-" .. num_w .. "d  %-" .. max_mob .. "s  %-" .. max_zone .. "s  %7s  %5s  %3d  %4s  %5s",
            i, trunc(k.mob_name, max_mob), trunc(k.zone, max_zone),
            format_number(k.xp_gained), format_number(k.damage_total),
            k.rounds, mlvl_str, time_str))
      end

      ColourNote("cyan", "", sep)

      local footer_label_w = num_w + 2 + max_mob
      local time_total_str = time_count > 0 and format_time(total_time) or "N/A"
      ColourNote("white", "", string.format(
         "  %-" .. footer_label_w .. "s  %-" .. max_zone .. "s  %7s  %5s  %3d  %4s  %5s",
         string.format("Tot %d kills", #kills), "",
         format_number(total_xp), format_number(total_damage),
         total_rounds, "", time_total_str))

      local avg_xp = math.floor(total_xp / #kills)
      local avg_dmg = math.floor(total_damage / #kills)
      local avg_rnd = string.format("%.1f", total_rounds / #kills)
      local avg_time_str = time_count > 0 and format_time(total_time / time_count) or "N/A"
      ColourNote("white", "", string.format(
         "  %-" .. footer_label_w .. "s  %-" .. max_zone .. "s  %7s  %5s  %3s  %4s  %5s",
         "Avg", "", format_number(avg_xp), format_number(avg_dmg),
         avg_rnd, "", avg_time_str))
   end

   -- Reward summary
   if pup_event.trains_earned then
      ColourNote("white", "", "  Trains earned: " .. pup_event.trains_earned)
   end
   if pup_event.xp_per_level then
      ColourNote("white", "", "  XP needed: " .. format_number(pup_event.xp_per_level))
   end
end

-- Show per-mob detail for a zone during pup leveling
local function show_pup_zone_detail(zone_search)
   local tier = cached_tier
   local redo = cached_redos or 0
   local remort = cached_remorts
   if not tier or not remort then
      ColourNote("red", "", PREFIX .. "Current tier/remort unknown. Try again after GMCP data arrives.")
      return
   end

   local zone_pattern = "%" .. zone_search .. "%"
   local query = string.format(
      "SELECT mob_name, COUNT(*) as cnt, AVG(xp_gained) as avg_xp, " ..
      "AVG(combat_time) as avg_time, AVG(rounds) as avg_rnd " ..
      "FROM kills WHERE level >= 200 AND tier = %d AND remort = %d AND %s " ..
      "AND zone LIKE %s " ..
      "AND (mob_level IS NULL OR mob_level > 200) " ..
      "GROUP BY mob_name ORDER BY cnt DESC",
      tier, remort, redo_where(redo), fixsql(zone_pattern))

   local mobs = {}
   for row in db:nrows(query) do
      table.insert(mobs, row)
   end

   -- Find actual zone name from first kill
   local actual_zone = zone_search
   for row in db:nrows(string.format(
      "SELECT zone FROM kills WHERE level >= 200 AND tier = %d AND remort = %d AND %s " ..
      "AND zone LIKE %s LIMIT 1", tier, remort, redo_where(redo), fixsql(zone_pattern))) do
      actual_zone = row.zone
   end

   local label = format_tier_label(tier, redo) .. " R" .. remort
   ColourNote("cyan", "", string.format("%sPup zone detail: %s (%s):", PREFIX, actual_zone, label))

   if #mobs == 0 then
      ColourNote("yellow", "", "  No pup kills found in this zone.")
      return
   end

   local fmt = "  %-24s  %5s  %5s  %7s  %5s"
   ColourNote("cyan", "", string.format(fmt, "Mob", "Kills", "AvgXP", "AvgTime", "AvgRd"))
   ColourNote("cyan", "", string.format(fmt, "------------------------", "-----", "-----", "-------", "-----"))

   for _, m in ipairs(mobs) do
      local mob_display = m.mob_name
      if #mob_display > 24 then mob_display = string.sub(mob_display, 1, 23) .. "+" end
      local avg_time_str = m.avg_time and format_time(m.avg_time) or "N/A"
      ColourNote("white", "", string.format(fmt,
         mob_display,
         tostring(m.cnt),
         format_abbreviated(math.floor(m.avg_xp)),
         avg_time_str,
         m.avg_rnd and string.format("%.1f", m.avg_rnd) or "-"))
   end
end

-- Main ldb pup command handler
function ldb_pup(name, line, wildcards)
   if not require_db() then return end
   local arg = wildcards[1]

   -- No argument: show overview for current T+R
   if not arg or arg == "" then
      show_pup_overview({})
      return
   end

   local s = string.match(arg, "^%s*(.-)%s*$")  -- trim
   local sl = string.lower(s)

   -- "list" or "list N": show recent pup events
   local list_n = string.match(sl, "^list%s*(%d*)$")
   if list_n ~= nil then
      local n = tonumber(list_n) or 10
      local tier = cached_tier
      local redo = cached_redos or 0
      local remort = cached_remorts
      if not tier or not remort then
         ColourNote("red", "", PREFIX .. "Current tier/remort unknown. Try again after GMCP data arrives.")
         return
      end
      local pup_where_clause = string.format("tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
      ColourNote("cyan", "", string.format("%sLast %d powerups (%s R%d):", PREFIX, n, format_tier_label(tier, redo), remort))
      render_pup_list(pup_where_clause, n)
      return
   end

   -- Numeric: show pup detail by ID
   local num = tonumber(s)
   if num then
      show_pup_detail(num)
      return
   end

   -- Try to parse as filter (all, T1, R3, T1R3, etc.)
   if sl == "all" or string.match(sl, "^[tr]%d") then
      local filter = parse_args(arg)
      if filter then
         show_pup_overview(filter)
      end
      return
   end

   -- Otherwise treat as zone substring
   show_pup_zone_detail(s)
end

function show_level_stats_section(level, tier, remort, redo, label)
   -- Build WHERE clause
   local where = string.format("level = %d", level)
   if tier then where = where .. string.format(" AND tier = %d", tier) end
   if remort then where = where .. string.format(" AND remort = %d", remort) end
   if redo then where = where .. " AND " .. redo_where(redo) end

   -- Header
   ColourNote("cyan", "", PREFIX .. "Level " .. level .. " kills " .. label .. ":")

   -- Fetch kills
   local kills_query = "SELECT mob_name, zone, xp_gained, damage_total, rounds, mob_level " ..
      "FROM kills WHERE " .. where .. " ORDER BY timestamp"
   local kills = {}
   for row in db:nrows(kills_query) do
      table.insert(kills, row)
   end

   -- Death count (deaths table has no pup column, filter by level + tier/remort/redo)
   local deaths_where = string.format("level = %d", level)
   if tier then deaths_where = deaths_where .. string.format(" AND tier = %d", tier) end
   if remort then deaths_where = deaths_where .. string.format(" AND remort = %d", remort) end
   if redo then deaths_where = deaths_where .. " AND " .. redo_where(redo) end
   local total_deaths = 0
   for row in db:nrows("SELECT COUNT(*) as cnt FROM deaths WHERE " .. deaths_where) do
      total_deaths = row.cnt
   end

   local no_kills_msg = "  No kills recorded at this level."
   if #kills == 0 then
      ColourNote("yellow", "", no_kills_msg)
      if total_deaths > 0 then
         ColourNote("red", "", "  Deaths: " .. total_deaths)
      end
      return
   end

   -- Calculate column widths from data
   local max_mob = 3   -- "Mob"
   local max_zone = 4  -- "Zone"
   for _, k in ipairs(kills) do
      if #k.mob_name > max_mob then max_mob = #k.mob_name end
      if #k.zone > max_zone then max_zone = #k.zone end
   end
   if max_mob > 24 then max_mob = 24 end
   if max_zone > 20 then max_zone = 20 end

   local num_w = #tostring(#kills)
   if num_w < 1 then num_w = 1 end

   local function trunc(s, w)
      if #s > w then return string.sub(s, 1, w - 1) .. "+" else return s end
   end

   -- Column header
   local hdr = string.format("  %-" .. num_w .. "s  %-" .. max_mob .. "s  %-" .. max_zone .. "s  %7s  %5s  %3s  %4s",
      "#", "Mob", "Zone", "XP", "Dmg", "Rnd", "MLvl")
   ColourNote("cyan", "", hdr)
   local sep = "  " .. string.rep("-", num_w) .. "  " .. string.rep("-", max_mob) .. "  " ..
      string.rep("-", max_zone) .. "  " .. string.rep("-", 7) .. "  " ..
      string.rep("-", 5) .. "  " .. string.rep("-", 3) .. "  " .. string.rep("-", 4)
   ColourNote("cyan", "", sep)

   -- Data rows
   local total_xp = 0
   local total_damage = 0
   local total_rounds = 0

   for i, k in ipairs(kills) do
      total_xp = total_xp + k.xp_gained
      total_damage = total_damage + k.damage_total
      total_rounds = total_rounds + k.rounds

      local mlvl_str = k.mob_level and tostring(k.mob_level) or "-"
      local row_str = string.format("  %-" .. num_w .. "d  %-" .. max_mob .. "s  %-" .. max_zone .. "s  %7s  %5s  %3d  %4s",
         i, trunc(k.mob_name, max_mob), trunc(k.zone, max_zone),
         format_number(k.xp_gained),
         format_number(k.damage_total), k.rounds, mlvl_str)
      ColourNote("white", "", row_str)
   end

   -- Footer
   ColourNote("cyan", "", sep)

   local footer_label_w = num_w + 2 + max_mob

   local tot_str = string.format("  %-" .. footer_label_w .. "s  %-" .. max_zone .. "s  %7s  %5s  %3d  %4s",
      string.format("Tot %d kills", #kills), "",
      format_number(total_xp),
      format_number(total_damage), total_rounds, "")
   ColourNote("white", "", tot_str)

   local avg_xp = math.floor(total_xp / #kills)
   local avg_dmg = math.floor(total_damage / #kills)
   local avg_rnd = string.format("%.1f", total_rounds / #kills)
   local avg_str = string.format("  %-" .. footer_label_w .. "s  %-" .. max_zone .. "s  %7s  %5s  %3s  %4s",
      "Avg", "",
      format_number(avg_xp),
      format_number(avg_dmg), avg_rnd, "")
   ColourNote("white", "", avg_str)

   if total_deaths > 0 then
      ColourNote("red", "", "  Deaths: " .. total_deaths)
   end
end

function show_level_stats(level, _, filter)
   filter = filter or {}

   -- Determine if this is a multi-section or single-section query
   local multi_section = filter.all or (filter.tier and not filter.remort)

   if multi_section then
      -- Build WHERE for combo discovery (exclude NULLs)
      local where = string.format("level = %d AND tier IS NOT NULL AND remort IS NOT NULL", level)
      if filter.tier then
         where = where .. string.format(" AND tier = %d", filter.tier)
         local redo = filter.redo or 0
         where = where .. " AND " .. redo_where(redo)
      end

      local combos = {}
      for row in db:nrows("SELECT DISTINCT tier, remort, COALESCE(redo, 0) as redo FROM kills WHERE " .. where ..
         " ORDER BY tier, COALESCE(redo, 0), remort") do
         table.insert(combos, {tier = row.tier, remort = row.remort, redo = row.redo})
      end

      if #combos == 0 then
         ColourNote("cyan", "", PREFIX .. "Level " .. level .. " kills:")
         ColourNote("yellow", "", "  No kills recorded at this level.")
         return
      end

      for i, combo in ipairs(combos) do
         if i > 1 then Note("") end
         local label = "(" .. format_tier_label(combo.tier, combo.redo) .. " R" .. combo.remort .. ")"
         show_level_stats_section(level, combo.tier, combo.remort, combo.redo, label)
      end
   else
      -- Single section: resolve tier and remort
      local tier = filter.tier or cached_tier
      local redo = filter.redo
      if not filter.tier then redo = cached_redos or 0 end
      if redo == nil then redo = 0 end
      local remort = filter.remort or cached_remorts

      if not tier then
         ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
         return
      end
      if not remort then
         ColourNote("red", "", PREFIX .. "Current remort unknown. Try again after GMCP data arrives.")
         return
      end

      local label = "(" .. format_tier_label(tier, redo) .. " R" .. remort .. ")"
      show_level_stats_section(level, tier, remort, redo, label)
   end
end

-- =============================================================================
-- Helper: query bracket stats for a given tier + remort
-- Returns table of bracket results, total kills, total deaths
-- =============================================================================
local function query_bracket_stats(tier, remort, redo)
   local results = {}
   local grand_kills = 0
   local grand_deaths = 0

   for _, br in ipairs(BRACKETS) do
      local where_k = string.format(
         "tier = %d AND remort = %d AND %s AND level >= %d AND level <= %d",
         tier, remort, redo_where(redo), br.min, br.max)
      local where_d = string.format(
         "tier = %d AND remort = %d AND %s AND level >= %d AND level <= %d",
         tier, remort, redo_where(redo), br.min, br.max)

      local row_data = {
         label = br.label,
         kills = 0, avg_xp = 0, avg_dmg = 0, avg_gap = nil,
         avg_rnd = 0, zones = 0, deaths = 0,
      }

      for row in db:nrows(
         "SELECT COUNT(*) as cnt, " ..
         "COALESCE(AVG(xp_gained), 0) as avg_xp, " ..
         "COALESCE(AVG(damage_total), 0) as avg_dmg, " ..
         "AVG(CASE WHEN mob_level IS NOT NULL THEN mob_level - level ELSE NULL END) as avg_gap, " ..
         "COALESCE(AVG(rounds), 0) as avg_rnd, " ..
         "COUNT(DISTINCT zone) as zones " ..
         "FROM kills WHERE " .. where_k) do
         row_data.kills = row.cnt
         row_data.avg_xp = row.avg_xp
         row_data.avg_dmg = row.avg_dmg
         row_data.avg_gap = row.avg_gap
         row_data.avg_rnd = row.avg_rnd
         row_data.zones = row.zones
      end

      for row in db:nrows("SELECT COUNT(*) as cnt FROM deaths WHERE " .. where_d) do
         row_data.deaths = row.cnt
      end

      grand_kills = grand_kills + row_data.kills
      grand_deaths = grand_deaths + row_data.deaths
      table.insert(results, row_data)
   end

   return results, grand_kills, grand_deaths
end

-- =============================================================================
-- Helper: render bracket table (used by both ldb remort and ldb tier)
-- =============================================================================
local function render_bracket_table(rows, show_header, grand_zones)
   local fmt =  "  %-7s  %5s  %5s  %6s  %6s  %6s  %5s  %6s"
   if show_header then
      ColourNote("cyan", "", string.format(fmt,
         "Bracket", "Kills", "AvgXP", "AvgDmg", "AvgGap", "AvgRnd", "Zones", "Deaths"))
      ColourNote("cyan", "", string.format(fmt,
         "-------", "-----", "-----", "------", "------", "------", "-----", "------"))
   end

   local grand_kills = 0
   local grand_xp_sum = 0
   local grand_dmg_sum = 0
   local grand_gap_sum = 0
   local grand_gap_count = 0
   local grand_rnd_sum = 0
   local grand_deaths = 0

   for _, r in ipairs(rows) do
      grand_kills = grand_kills + r.kills
      grand_deaths = grand_deaths + r.deaths

      if r.kills == 0 then
         ColourNote("white", "", string.format(fmt,
            r.label, "-", "-", "-", "-", "-", "-",
            r.deaths > 0 and tostring(r.deaths) or "-"))
      else
         grand_xp_sum = grand_xp_sum + (r.avg_xp * r.kills)
         grand_dmg_sum = grand_dmg_sum + (r.avg_dmg * r.kills)
         grand_rnd_sum = grand_rnd_sum + (r.avg_rnd * r.kills)
         if r.avg_gap then
            grand_gap_sum = grand_gap_sum + (r.avg_gap * r.kills)
            grand_gap_count = grand_gap_count + r.kills
         end

         local gap_str = "-"
         if r.avg_gap then
            gap_str = string.format("%+.1f", r.avg_gap)
         end

         ColourNote("white", "", string.format(fmt,
            r.label,
            format_abbreviated(r.kills),
            format_abbreviated(math.floor(r.avg_xp)),
            format_abbreviated(math.floor(r.avg_dmg)),
            gap_str,
            string.format("%.1f", r.avg_rnd),
            r.zones,
            r.deaths > 0 and tostring(r.deaths) or "-"))
      end
   end

   -- Total row
   ColourNote("cyan", "", string.format(fmt,
      "-------", "-----", "-----", "------", "------", "------", "-----", "------"))

   local tot_avg_xp = grand_kills > 0 and format_abbreviated(math.floor(grand_xp_sum / grand_kills)) or "-"
   local tot_avg_dmg = grand_kills > 0 and format_abbreviated(math.floor(grand_dmg_sum / grand_kills)) or "-"
   local tot_avg_gap = grand_gap_count > 0 and string.format("%+.1f", grand_gap_sum / grand_gap_count) or "-"
   local tot_avg_rnd = grand_kills > 0 and string.format("%.1f", grand_rnd_sum / grand_kills) or "-"

   local zones_str = grand_zones and tostring(grand_zones) or ""
   ColourNote("white", "", string.format(fmt,
      "Total",
      format_abbreviated(grand_kills),
      tot_avg_xp,
      tot_avg_dmg,
      tot_avg_gap,
      tot_avg_rnd,
      zones_str,
      grand_deaths > 0 and tostring(grand_deaths) or "-"))
end

-- =============================================================================
-- Command: ldb remort [R] [T<n>]
-- =============================================================================
function ldb_remort(name, line, wildcards)
   if not require_db() then return end

   local args = parse_args(wildcards[1])
   if not args then return end

   local tier = args.tier or cached_tier
   local redo = args.redo
   if not args.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end
   local remort = args.remort or cached_remorts

   if not tier then
      ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
      return
   end
   if not remort then
      ColourNote("red", "", PREFIX .. "Current remort unknown. Try again after GMCP data arrives.")
      return
   end

   local results, grand_kills, grand_deaths = query_bracket_stats(tier, remort, redo)

   -- Total distinct zones across all brackets (a zone can span multiple brackets)
   local grand_zones = 0
   for row in db:nrows(string.format(
      "SELECT COUNT(DISTINCT zone) as cnt FROM kills WHERE tier = %d AND remort = %d AND %s " ..
      "AND level >= 1 AND level <= 200",
      tier, remort, redo_where(redo))) do
      grand_zones = row.cnt
   end

   -- Header
   local death_str = grand_deaths > 0 and (", " .. grand_deaths .. " deaths") or ""
   ColourNote("cyan", "", string.format(
      "%sRemort summary - %s R%d (%s kills%s):",
      PREFIX, format_tier_label(tier, redo), remort, format_number(grand_kills), death_str))

   if grand_kills == 0 then
      ColourNote("yellow", "", "  No kills recorded for this remort.")
      return
   end

   render_bracket_table(results, true, grand_zones)

   -- Pup section
   local pup_where_clause = string.format("tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
   local kill_where = string.format("level >= 200 AND tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
   -- render_pup_section prints nothing if no pup data exists
   render_pup_section(pup_where_clause, kill_where)
end

-- =============================================================================
-- Command: ldb tier [T]
-- =============================================================================
function ldb_tier(name, line, wildcards)
   if not require_db() then return end

   local args = parse_args(wildcards[1], {bare_is_tier = true})
   if not args then return end

   local tier = args.tier or cached_tier
   local redo = args.redo
   if not args.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end

   if not tier then
      ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
      return
   end

   -- Find all remorts for this tier+redo
   local remorts = {}
   for row in db:nrows(string.format(
      "SELECT DISTINCT remort FROM kills WHERE tier = %d AND %s AND remort IS NOT NULL " ..
      "ORDER BY remort", tier, redo_where(redo))) do
      table.insert(remorts, row.remort)
   end

   if #remorts == 0 then
      ColourNote("cyan", "", string.format("%sTier summary - %s:", PREFIX, format_tier_label(tier, redo)))
      ColourNote("yellow", "", "  No kills recorded for this tier.")
      return
   end

   -- Count totals for header
   local tier_kills = 0
   local tier_deaths = 0
   for row in db:nrows(string.format(
      "SELECT COUNT(*) as cnt FROM kills WHERE tier = %d AND %s AND remort IS NOT NULL", tier, redo_where(redo))) do
      tier_kills = row.cnt
   end
   for row in db:nrows(string.format(
      "SELECT COUNT(*) as cnt FROM deaths WHERE tier = %d AND %s AND remort IS NOT NULL", tier, redo_where(redo))) do
      tier_deaths = row.cnt
   end

   local death_str = tier_deaths > 0 and (", " .. tier_deaths .. " deaths") or ""
   ColourNote("cyan", "", string.format(
      "%sTier summary - %s (%d remorts, %s kills%s):",
      PREFIX, format_tier_label(tier, redo), #remorts, format_number(tier_kills), death_str))

   -- Query bracket stats for all remorts up front (reuse query_bracket_stats)
   local all_remort_data = {}
   for _, rm in ipairs(remorts) do
      all_remort_data[rm] = query_bracket_stats(tier, rm, redo)
   end

   -- For each bracket, show a section with one row per remort
   local tier_fmt = "  %-7s  %5s  %5s  %6s  %6s  %6s  %5s  %6s"
   for bi, br in ipairs(BRACKETS) do
      if bi > 1 then Note("") end

      -- Collect the bi-th bracket row from each remort's results
      local bracket_has_data = false
      local bracket_rows = {}
      for _, rm in ipairs(remorts) do
         local r = all_remort_data[rm][bi]
         local row_data = {
            label = "R" .. rm,
            kills = r.kills, avg_xp = r.avg_xp, avg_dmg = r.avg_dmg,
            avg_gap = r.avg_gap, avg_rnd = r.avg_rnd, zones = r.zones,
            deaths = r.deaths,
         }
         if row_data.kills > 0 or row_data.deaths > 0 then
            bracket_has_data = true
         end
         table.insert(bracket_rows, row_data)
      end

      ColourNote("cyan", "", "  -- " .. br.label .. " --")
      if not bracket_has_data then
         ColourNote("yellow", "", "    No data.")
      else
         ColourNote("cyan", "", string.format(tier_fmt,
            "Remort", "Kills", "AvgXP", "AvgDmg", "AvgGap", "AvgRnd", "Zones", "Deaths"))
         ColourNote("cyan", "", string.format(tier_fmt,
            "-------", "-----", "-----", "------", "------", "------", "-----", "------"))

         for _, r in ipairs(bracket_rows) do
            if r.kills == 0 then
               ColourNote("white", "", string.format(tier_fmt,
                  r.label, "-", "-", "-", "-", "-", "-",
                  r.deaths > 0 and tostring(r.deaths) or "-"))
            else
               local gap_str = "-"
               if r.avg_gap then
                  gap_str = string.format("%+.1f", r.avg_gap)
               end

               ColourNote("white", "", string.format(tier_fmt,
                  r.label,
                  format_abbreviated(r.kills),
                  format_abbreviated(math.floor(r.avg_xp)),
                  format_abbreviated(math.floor(r.avg_dmg)),
                  gap_str,
                  string.format("%.1f", r.avg_rnd),
                  r.zones,
                  r.deaths > 0 and tostring(r.deaths) or "-"))
            end
         end
      end
   end

   -- Powerups comparison section
   local pup_fmt = "  %-7s  %4s  %6s  %7s  %7s  %6s"
   local pup_has_data = false
   local pup_rows = {}
   for _, rm in ipairs(remorts) do
      local pup_row = {label = "R" .. rm, pups = 0, trains = 0,
         trn_pup = nil, avg_time = nil, trn_hr = nil}

      for row in db:nrows(string.format(
         "SELECT COUNT(*) as cnt, COALESCE(SUM(trains_earned), 0) as trains, " ..
         "AVG(trains_earned) as avg_t " ..
         "FROM pup_events WHERE tier = %d AND remort = %d AND %s", tier, rm, redo_where(redo))) do
         pup_row.pups = row.cnt
         pup_row.trains = row.trains
         pup_row.trn_pup = row.avg_t
      end

      if pup_row.pups > 0 then
         pup_has_data = true
         -- Get avg combat time per pup
         local total_combat_time = 0
         for row in db:nrows(string.format(
            "SELECT COALESCE(SUM(combat_time), 0) as t FROM kills WHERE level >= 200 " ..
            "AND tier = %d AND remort = %d AND %s AND combat_time IS NOT NULL " ..
            "AND (mob_level IS NULL OR mob_level > 200)", tier, rm, redo_where(redo))) do
            total_combat_time = row.t
         end
         pup_row.avg_time = total_combat_time / pup_row.pups
         if pup_row.trn_pup and pup_row.avg_time > 0 then
            pup_row.trn_hr = math.floor(pup_row.trn_pup / pup_row.avg_time * 3600)
         end
      end

      table.insert(pup_rows, pup_row)
   end

   if pup_has_data then
      Note("")
      ColourNote("cyan", "", "  -- Powerups --")
      ColourNote("cyan", "", string.format(pup_fmt,
         "Remort", "Pups", "Trains", "Trn/Pup", "AvgTime", "Trn/Hr"))
      ColourNote("cyan", "", string.format(pup_fmt,
         "------", "----", "------", "-------", "-------", "------"))

      for _, r in ipairs(pup_rows) do
         if r.pups == 0 then
            ColourNote("white", "", string.format(pup_fmt,
               r.label, "-", "-", "-", "-", "-"))
         else
            ColourNote("white", "", string.format(pup_fmt,
               r.label,
               tostring(r.pups),
               tostring(r.trains),
               r.trn_pup and string.format("%.1f", r.trn_pup) or "N/A",
               format_time(r.avg_time),
               r.trn_hr and tostring(r.trn_hr) or "N/A"))
         end
      end
   end
end

-- =============================================================================
-- Helper: format duration as seconds or minutes
-- =============================================================================
local function format_duration(secs)
   if not secs then return "-" end
   if secs < 120 then
      return secs .. "s"
   else
      return math.floor(secs / 60) .. "m"
   end
end

-- =============================================================================
-- Command: ldb quest / ldb quests [filter]
-- =============================================================================
function ldb_quests(name, line, wildcards)
   if not require_db() then return end

   local filter = parse_args(wildcards[1])
   if not filter then return end

   -- Resolve tier/remort/redo
   local tier = filter.tier or cached_tier
   local redo = filter.redo
   if not filter.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end
   local remort = filter.remort or cached_remorts

   if not filter.all then
      if not tier then
         ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
         return
      end
      if not remort then
         ColourNote("red", "", PREFIX .. "Current remort unknown. Try again after GMCP data arrives.")
         return
      end
   end

   -- Build WHERE clause
   local where = "1=1"
   if not filter.all then
      where = string.format("tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
   elseif filter.tier then
      where = string.format("tier = %d AND %s", filter.tier, redo_where(filter.redo or 0))
   end

   -- Fetch quests in chronological order (cap at 50)
   local quests = {}
   for row in db:nrows(
      "SELECT id, timestamp, level, mob_name, area, result, qp, gold, tp, duration " ..
      "FROM quests WHERE " .. where .. " ORDER BY timestamp LIMIT 50") do
      table.insert(quests, row)
   end

   -- Check if there are more
   local total_count = 0
   for row in db:nrows("SELECT COUNT(*) as cnt FROM quests WHERE " .. where) do
      total_count = row.cnt
   end

   -- Header
   local label = filter.all and "all" or (format_tier_label(tier, redo) .. " R" .. remort)
   ColourNote("cyan", "", PREFIX .. "Quests - " .. label .. ":")

   if #quests == 0 then
      ColourNote("yellow", "", "  No quests recorded.")
      return
   end

   -- Determine max mob+area width
   local max_mob = 10  -- minimum
   for _, q in ipairs(quests) do
      local mob_area = (q.mob_name or "?") .. " (" .. (q.area or "?") .. ")"
      if #mob_area > max_mob then max_mob = #mob_area end
   end
   if max_mob > 35 then max_mob = 35 end

   local function trunc(s, w)
      if #s > w then return string.sub(s, 1, w - 1) .. "+" else return s end
   end

   -- Column header (# = database ID)
   local max_id = 2
   for _, q in ipairs(quests) do
      local id_len = #tostring(q.id)
      if id_len > max_id then max_id = id_len end
   end
   local fmt = "  %-" .. max_id .. "s  %3s  %4s  %5s  %3s  %5s  %-" .. max_mob .. "s"
   ColourNote("cyan", "", string.format(fmt,
      "#", "Lvl", "QP", "Gold", "TP", "Time", "Mob (Area)"))
   local sep = string.format(fmt,
      string.rep("-", max_id), "---", "----", "-----", "---", "-----",
      string.rep("-", max_mob))
   ColourNote("cyan", "", sep)

   -- Data rows
   local comp_count = 0
   local fail_count = 0
   local timeout_count = 0
   local unknown_count = 0
   local sum_qp = 0
   local sum_gold = 0
   local sum_duration = 0

   for i, q in ipairs(quests) do
      local is_active = (q.id == active_quest_id and (q.result == nil or q.result == ""))
      local is_failed = (q.result == "failed" or q.result == "timeout")

      if q.result == "completed" then
         comp_count = comp_count + 1
         sum_qp = sum_qp + (q.qp or 0)
         sum_gold = sum_gold + (q.gold or 0)
         sum_duration = sum_duration + (q.duration or 0)
      elseif q.result == "failed" then
         fail_count = fail_count + 1
      elseif q.result == "timeout" then
         timeout_count = timeout_count + 1
      elseif q.result == "unknown" then
         unknown_count = unknown_count + 1
      end

      local mob_area = (q.mob_name or "?") .. " (" .. (q.area or "?") .. ")"

      local row_text = string.format(fmt,
         q.id,
         q.level,
         q.qp and format_number(q.qp) or "-",
         q.gold and format_number(q.gold) or "-",
         q.tp and tostring(q.tp) or "-",
         format_duration(q.duration),
         trunc(mob_area, max_mob))

      if is_active then
         ColourNote("white", "", row_text, "green", "", " (active)")
      elseif is_failed then
         ColourNote("white", "", row_text, "red", "", " (" .. q.result .. ")")
      else
         ColourNote("white", "", row_text)
      end
   end

   -- Summary
   ColourNote("cyan", "", sep)
   local parts = {}
   if comp_count > 0 then table.insert(parts, comp_count .. " completed") end
   if fail_count > 0 then table.insert(parts, fail_count .. " failed") end
   if timeout_count > 0 then table.insert(parts, timeout_count .. " timeout") end
   if unknown_count > 0 then table.insert(parts, unknown_count .. " unknown") end
   local summary_line = "  " .. table.concat(parts, ", ")
   if total_count > #quests then
      summary_line = summary_line .. string.format(" (showing %d of %d)", #quests, total_count)
   else
      summary_line = summary_line .. string.format(" (of %d total)", #quests)
   end
   ColourNote("white", "", summary_line)

   if comp_count > 0 then
      local avg_qp = math.floor(sum_qp / comp_count)
      local avg_dur = math.floor(sum_duration / comp_count)
      ColourNote("white", "", string.format("  Avg QP: %s | Avg time: %s | Total QP: %s | Total gold: %s",
         format_number(avg_qp), format_duration(avg_dur),
         format_number(sum_qp), format_number(sum_gold)))
   end
end

-- =============================================================================
-- Command: ldb cp / ldb cps / ldb campaign / ldb campaigns [filter]
-- =============================================================================
function ldb_campaigns(name, line, wildcards)
   if not require_db() then return end

   local filter = parse_args(wildcards[1])
   if not filter then return end

   -- Resolve tier/remort/redo
   local tier = filter.tier or cached_tier
   local redo = filter.redo
   if not filter.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end
   local remort = filter.remort or cached_remorts

   if not filter.all then
      if not tier then
         ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
         return
      end
      if not remort then
         ColourNote("red", "", PREFIX .. "Current remort unknown. Try again after GMCP data arrives.")
         return
      end
   end

   -- Build WHERE clause
   local where = "1=1"
   if not filter.all then
      where = string.format("tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
   elseif filter.tier then
      where = string.format("tier = %d AND %s", filter.tier, redo_where(filter.redo or 0))
   end

   -- Fetch campaigns in chronological order (cap at 50)
   local campaigns = {}
   for row in db:nrows(
      "SELECT id, timestamp, level, result, mob_count, qp, gold, tp, duration " ..
      "FROM campaigns WHERE " .. where .. " ORDER BY timestamp LIMIT 50") do
      table.insert(campaigns, row)
   end

   -- Check if there are more
   local total_count = 0
   for row in db:nrows("SELECT COUNT(*) as cnt FROM campaigns WHERE " .. where) do
      total_count = row.cnt
   end

   -- Header
   local label = filter.all and "all" or (format_tier_label(tier, redo) .. " R" .. remort)
   ColourNote("cyan", "", PREFIX .. "Campaigns - " .. label .. ":")

   if #campaigns == 0 then
      ColourNote("yellow", "", "  No campaigns recorded.")
      return
   end

   -- Column header (# = database ID)
   local max_id = 2
   for _, c in ipairs(campaigns) do
      local id_len = #tostring(c.id)
      if id_len > max_id then max_id = id_len end
   end
   local fmt = "  %-" .. max_id .. "s  %3s  %4s  %4s  %5s  %3s  %5s"
   ColourNote("cyan", "", string.format(fmt,
      "#", "Lvl", "Mobs", "QP", "Gold", "TP", "Time"))
   local sep = string.format(fmt,
      string.rep("-", max_id), "---", "----", "----", "-----", "---", "-----")
   ColourNote("cyan", "", sep)

   -- Data rows
   local comp_count = 0
   local quit_count = 0
   local unknown_count = 0
   local sum_qp = 0
   local sum_gold = 0
   local sum_duration = 0

   for i, c in ipairs(campaigns) do
      local is_active = (c.id == active_campaign_id and (c.result == nil or c.result == ""))
      local is_quit = (c.result == "quit")

      if c.result == "completed" then
         comp_count = comp_count + 1
         sum_qp = sum_qp + (c.qp or 0)
         sum_gold = sum_gold + (c.gold or 0)
         sum_duration = sum_duration + (c.duration or 0)
      elseif c.result == "quit" then
         quit_count = quit_count + 1
      elseif c.result == "unknown" then
         unknown_count = unknown_count + 1
      end

      local row_text = string.format(fmt,
         c.id,
         c.level,
         c.mob_count and tostring(c.mob_count) or "-",
         c.qp and format_number(c.qp) or "-",
         c.gold and format_number(c.gold) or "-",
         c.tp and tostring(c.tp) or "-",
         format_duration(c.duration))

      if is_active then
         ColourNote("white", "", row_text, "green", "", " (active)")
      elseif is_quit then
         ColourNote("white", "", row_text, "red", "", " (quit)")
      else
         ColourNote("white", "", row_text)
      end
   end

   -- Summary
   ColourNote("cyan", "", sep)
   local parts = {}
   if comp_count > 0 then table.insert(parts, comp_count .. " completed") end
   if quit_count > 0 then table.insert(parts, quit_count .. " quit") end
   if unknown_count > 0 then table.insert(parts, unknown_count .. " unknown") end
   local summary_line = "  " .. table.concat(parts, ", ")
   if total_count > #campaigns then
      summary_line = summary_line .. string.format(" (showing %d of %d)", #campaigns, total_count)
   else
      summary_line = summary_line .. string.format(" (of %d total)", #campaigns)
   end
   ColourNote("white", "", summary_line)

   if comp_count > 0 then
      local avg_qp = math.floor(sum_qp / comp_count)
      local avg_dur = math.floor(sum_duration / comp_count)
      ColourNote("white", "", string.format("  Avg QP: %s | Avg time: %s | Total QP: %s | Total gold: %s",
         format_number(avg_qp), format_duration(avg_dur),
         format_number(sum_qp), format_number(sum_gold)))
   end
end

-- =============================================================================
-- Command: ldb cp show <id>
-- =============================================================================
function ldb_campaign_show(name, line, wildcards)
   if not require_db() then return end

   local cp_id = tonumber(wildcards[1])
   if not cp_id then
      ColourNote("red", "", PREFIX .. "Usage: ldb cp show <id>")
      return
   end

   -- Fetch campaign record
   local campaign = nil
   for row in db:nrows(string.format(
      "SELECT id, timestamp, level, tier, remort, redo, result, duration, mob_count, " ..
      "qp, gold, tp, trains, pracs, expected_qp, expected_gold " ..
      "FROM campaigns WHERE id = %d", cp_id)) do
      campaign = row
   end

   if not campaign then
      ColourNote("red", "", PREFIX .. "Campaign #" .. cp_id .. " not found.")
      return
   end

   -- Header
   ColourNote("cyan", "", PREFIX .. "Campaign #" .. cp_id .. ":")

   -- Started
   ColourNote("white", "", "  Started:  " .. os.date("%Y-%m-%d %H:%M", campaign.timestamp))

   -- Level + tier/remort
   local level_str = tostring(campaign.level)
   if campaign.tier and campaign.remort then
      level_str = level_str .. " (" .. format_tier_label(campaign.tier, campaign.redo) .. " R" .. campaign.remort .. ")"
   end
   ColourNote("white", "", "  Level:    " .. level_str)

   -- Result + duration
   local is_active = (campaign.id == active_campaign_id and (campaign.result == nil or campaign.result == ""))
   local result = campaign.result or "active"
   local dur_str = campaign.duration and (" (" .. format_duration(campaign.duration) .. ")") or ""

   if is_active or result == "active" then
      ColourNote("white", "", "  Result:   ", "green", "", "active")
   elseif result == "quit" then
      ColourNote("white", "", "  Result:   ", "red", "", "quit" .. dur_str)
   elseif result == "unknown" then
      ColourNote("white", "", "  Result:   ", "red", "", "unknown" .. dur_str)
   elseif result == "completed" then
      ColourNote("white", "", "  Result:   completed" .. dur_str)
   else
      ColourNote("white", "", "  Result:   " .. result .. dur_str)
   end

   -- Mobs
   if campaign.mob_count then
      ColourNote("white", "", "  Mobs:     " .. campaign.mob_count)
   end

   -- Rewards
   if result == "completed" then
      local reward_parts = {}
      if campaign.qp then table.insert(reward_parts, format_number(campaign.qp) .. " QP") end
      if campaign.gold then table.insert(reward_parts, format_number(campaign.gold) .. " gold") end
      if campaign.tp then table.insert(reward_parts, campaign.tp .. " TP") end
      if campaign.trains then table.insert(reward_parts, campaign.trains .. " trains") end
      if campaign.pracs then table.insert(reward_parts, campaign.pracs .. " pracs") end
      if #reward_parts > 0 then
         ColourNote("white", "", "  Rewards:  " .. table.concat(reward_parts, ", "))
      end
   elseif is_active or result == "active" then
      -- Show expected rewards for active campaigns
      local exp_parts = {}
      if campaign.expected_qp then table.insert(exp_parts, format_number(campaign.expected_qp) .. " QP") end
      if campaign.expected_gold then table.insert(exp_parts, format_number(campaign.expected_gold) .. " gold") end
      if #exp_parts > 0 then
         ColourNote("white", "", "  Expected: " .. table.concat(exp_parts, ", "))
      end
   end

   -- Mob list
   local mobs = {}
   for row in db:nrows(string.format(
      "SELECT mob_name, area FROM campaign_mobs WHERE campaign_id = %d ORDER BY id", cp_id)) do
      table.insert(mobs, row)
   end

   if #mobs > 0 then
      Note("")
      ColourNote("cyan", "", "  Mob list (" .. #mobs .. "):")
      local num_w = #tostring(#mobs)
      for i, mob in ipairs(mobs) do
         ColourNote("white", "", string.format("    %" .. num_w .. "d. %s (%s)",
            i, mob.mob_name, mob.area or "?"))
      end
   end
end

-- =============================================================================
-- Command: ldb gq / ldb gquests [filter]
-- =============================================================================
function ldb_gquests(name, line, wildcards)
   if not require_db() then return end

   local filter = parse_args(wildcards[1])
   if not filter then return end

   -- Resolve tier/remort/redo
   local tier = filter.tier or cached_tier
   local redo = filter.redo
   if not filter.tier then redo = cached_redos or 0 end
   if redo == nil then redo = 0 end
   local remort = filter.remort or cached_remorts

   if not filter.all then
      if not tier then
         ColourNote("red", "", PREFIX .. "Current tier unknown. Try again after GMCP data arrives.")
         return
      end
      if not remort then
         ColourNote("red", "", PREFIX .. "Current remort unknown. Try again after GMCP data arrives.")
         return
      end
   end

   -- Build WHERE clause
   local where = "1=1"
   if not filter.all then
      where = string.format("tier = %d AND remort = %d AND %s", tier, remort, redo_where(redo))
   elseif filter.tier then
      where = string.format("tier = %d AND %s", filter.tier, redo_where(filter.redo or 0))
   end

   -- Fetch gquests in chronological order (cap at 50)
   local gquests = {}
   for row in db:nrows(
      "SELECT id, timestamp, level, gq_number, result, mob_count, qp, gold, tp, duration " ..
      "FROM gquests WHERE " .. where .. " ORDER BY timestamp LIMIT 50") do
      table.insert(gquests, row)
   end

   -- Check if there are more
   local total_count = 0
   for row in db:nrows("SELECT COUNT(*) as cnt FROM gquests WHERE " .. where) do
      total_count = row.cnt
   end

   -- Header
   local label = filter.all and "all" or (format_tier_label(tier, redo) .. " R" .. remort)
   ColourNote("cyan", "", PREFIX .. "Global Quests - " .. label .. ":")

   if #gquests == 0 then
      ColourNote("yellow", "", "  No global quests recorded.")
      return
   end

   -- Column header (# = database ID)
   local max_id = 2
   for _, g in ipairs(gquests) do
      local id_len = #tostring(g.id)
      if id_len > max_id then max_id = id_len end
   end
   local fmt = "  %-" .. max_id .. "s  %3s  %4s  %4s  %5s  %3s  %5s"
   ColourNote("cyan", "", string.format(fmt,
      "#", "Lvl", "Mobs", "QP", "Gold", "TP", "Time"))
   local sep = string.format(fmt,
      string.rep("-", max_id), "---", "----", "----", "-----", "---", "-----")
   ColourNote("cyan", "", sep)

   -- Data rows
   local won_count = 0
   local comp_count = 0
   local expired_count = 0
   local cancelled_count = 0
   local quit_count = 0
   local unknown_count = 0
   local sum_qp = 0
   local sum_gold = 0
   local sum_duration = 0
   local finished_count = 0  -- won + completed (for avg calculations)

   for i, g in ipairs(gquests) do
      local is_active = (g.id == active_gquest_id and (g.result == nil or g.result == ""))

      if g.result == "won" then
         won_count = won_count + 1
         finished_count = finished_count + 1
         sum_qp = sum_qp + (g.qp or 0)
         sum_gold = sum_gold + (g.gold or 0)
         sum_duration = sum_duration + (g.duration or 0)
      elseif g.result == "completed" then
         comp_count = comp_count + 1
         finished_count = finished_count + 1
         sum_qp = sum_qp + (g.qp or 0)
         sum_gold = sum_gold + (g.gold or 0)
         sum_duration = sum_duration + (g.duration or 0)
      elseif g.result == "expired" then
         expired_count = expired_count + 1
         sum_qp = sum_qp + (g.qp or 0)  -- per-mob QP still counts
      elseif g.result == "cancelled" then
         cancelled_count = cancelled_count + 1
         sum_qp = sum_qp + (g.qp or 0)
      elseif g.result == "quit" then
         quit_count = quit_count + 1
         sum_qp = sum_qp + (g.qp or 0)
      elseif g.result == "unknown" then
         unknown_count = unknown_count + 1
      end

      local row_text = string.format(fmt,
         g.id,
         g.level,
         g.mob_count and tostring(g.mob_count) or "-",
         g.qp and format_number(g.qp) or "-",
         g.gold and format_number(g.gold) or "-",
         g.tp and tostring(g.tp) or "-",
         format_duration(g.duration))

      if is_active then
         ColourNote("white", "", row_text, "green", "", " (active)")
      elseif g.result == "won" then
         ColourNote("white", "", row_text, "cyan", "", " (won)")
      elseif g.result == "expired" then
         ColourNote("white", "", row_text, "red", "", " (expired)")
      elseif g.result == "cancelled" then
         ColourNote("white", "", row_text, "red", "", " (cancelled)")
      elseif g.result == "quit" then
         ColourNote("white", "", row_text, "red", "", " (quit)")
      else
         ColourNote("white", "", row_text)
      end
   end

   -- Summary
   ColourNote("cyan", "", sep)
   local parts = {}
   if won_count > 0 then table.insert(parts, won_count .. " won") end
   if comp_count > 0 then table.insert(parts, comp_count .. " completed") end
   if expired_count > 0 then table.insert(parts, expired_count .. " expired") end
   if cancelled_count > 0 then table.insert(parts, cancelled_count .. " cancelled") end
   if quit_count > 0 then table.insert(parts, quit_count .. " quit") end
   if unknown_count > 0 then table.insert(parts, unknown_count .. " unknown") end
   local summary_line = "  " .. table.concat(parts, ", ")
   if total_count > #gquests then
      summary_line = summary_line .. string.format(" (showing %d of %d)", #gquests, total_count)
   else
      summary_line = summary_line .. string.format(" (of %d total)", #gquests)
   end
   ColourNote("white", "", summary_line)

   if sum_qp > 0 then
      local avg_parts = {}
      if finished_count > 0 then
         table.insert(avg_parts, "Avg QP: " .. format_number(math.floor(sum_qp / finished_count)))
         table.insert(avg_parts, "Avg time: " .. format_duration(math.floor(sum_duration / finished_count)))
      end
      table.insert(avg_parts, "Total QP: " .. format_number(sum_qp))
      if sum_gold > 0 then
         table.insert(avg_parts, "Total gold: " .. format_number(sum_gold))
      end
      ColourNote("white", "", "  " .. table.concat(avg_parts, " | "))
   end
end

-- =============================================================================
-- Command: ldb gq show <id>
-- =============================================================================
function ldb_gquest_show(name, line, wildcards)
   if not require_db() then return end

   local gq_id = tonumber(wildcards[1])
   if not gq_id then
      ColourNote("red", "", PREFIX .. "Usage: ldb gq show <id>")
      return
   end

   -- Fetch gquest record
   local gquest = nil
   for row in db:nrows(string.format(
      "SELECT id, timestamp, gq_number, level, tier, remort, redo, result, duration, mob_count, " ..
      "qp, gold, tp, trains, pracs, expected_qp, expected_gold " ..
      "FROM gquests WHERE id = %d", gq_id)) do
      gquest = row
   end

   if not gquest then
      ColourNote("red", "", PREFIX .. "Global Quest #" .. gq_id .. " not found.")
      return
   end

   -- Header
   local header = PREFIX .. "Global Quest #" .. gq_id
   if gquest.gq_number then
      header = header .. " (GQ " .. gquest.gq_number .. ")"
   end
   ColourNote("cyan", "", header .. ":")

   -- Started
   ColourNote("white", "", "  Started:  " .. os.date("%Y-%m-%d %H:%M", gquest.timestamp))

   -- Level + tier/remort
   local level_str = tostring(gquest.level)
   if gquest.tier and gquest.remort then
      level_str = level_str .. " (" .. format_tier_label(gquest.tier, gquest.redo) .. " R" .. gquest.remort .. ")"
   end
   ColourNote("white", "", "  Level:    " .. level_str)

   -- Result + duration
   local is_active = (gquest.id == active_gquest_id and (gquest.result == nil or gquest.result == ""))
   local result = gquest.result or "active"
   local dur_str = gquest.duration and (" (" .. format_duration(gquest.duration) .. ")") or ""

   if is_active or result == "active" then
      ColourNote("white", "", "  Result:   ", "green", "", "active")
   elseif result == "won" then
      ColourNote("white", "", "  Result:   ", "cyan", "", "won" .. dur_str)
   elseif result == "completed" then
      ColourNote("white", "", "  Result:   completed" .. dur_str)
   elseif result == "expired" then
      ColourNote("white", "", "  Result:   ", "red", "", "expired" .. dur_str)
   elseif result == "cancelled" then
      ColourNote("white", "", "  Result:   ", "red", "", "cancelled" .. dur_str)
   elseif result == "quit" then
      ColourNote("white", "", "  Result:   ", "red", "", "quit" .. dur_str)
   elseif result == "unknown" then
      ColourNote("white", "", "  Result:   ", "red", "", "unknown" .. dur_str)
   else
      ColourNote("white", "", "  Result:   " .. result .. dur_str)
   end

   -- Mobs
   if gquest.mob_count then
      ColourNote("white", "", "  Mobs:     " .. gquest.mob_count)
   end

   -- Rewards
   if result == "won" or result == "completed" then
      local reward_parts = {}
      if gquest.qp then table.insert(reward_parts, format_number(gquest.qp) .. " QP") end
      if gquest.gold then table.insert(reward_parts, format_number(gquest.gold) .. " gold") end
      if gquest.tp then table.insert(reward_parts, gquest.tp .. " TP") end
      if gquest.trains then table.insert(reward_parts, gquest.trains .. " trains") end
      if gquest.pracs then table.insert(reward_parts, gquest.pracs .. " pracs") end
      if #reward_parts > 0 then
         ColourNote("white", "", "  Rewards:  " .. table.concat(reward_parts, ", "))
      end
   elseif is_active or result == "active" then
      -- Show expected rewards and per-mob QP accumulated so far
      local exp_parts = {}
      if gquest.expected_qp then table.insert(exp_parts, format_number(gquest.expected_qp) .. " QP (base)") end
      if gquest.expected_gold then table.insert(exp_parts, format_number(gquest.expected_gold) .. " gold") end
      if #exp_parts > 0 then
         ColourNote("white", "", "  Expected: " .. table.concat(exp_parts, ", "))
      end
      if gquest.qp and gquest.qp > 0 then
         ColourNote("white", "", "  Mob QP:   " .. format_number(gquest.qp) .. " (so far)")
      end
   elseif result == "expired" or result == "cancelled" or result == "quit" then
      -- Show per-mob QP earned even though gquest wasn't completed
      if gquest.qp and gquest.qp > 0 then
         ColourNote("white", "", "  Mob QP:   " .. format_number(gquest.qp))
      end
   end

   -- Mob list
   local mobs = {}
   for row in db:nrows(string.format(
      "SELECT mob_name, area, mob_count FROM gquest_mobs WHERE gquest_id = %d ORDER BY id", gq_id)) do
      table.insert(mobs, row)
   end

   if #mobs > 0 then
      Note("")
      ColourNote("cyan", "", "  Mob list (" .. #mobs .. "):")
      local num_w = #tostring(#mobs)
      for i, mob in ipairs(mobs) do
         local mob_str = mob.mob_name
         if mob.mob_count and mob.mob_count > 1 then
            mob_str = mob.mob_count .. " * " .. mob_str
         end
         ColourNote("white", "", string.format("    %" .. num_w .. "d. %s (%s)",
            i, mob_str, mob.area or "?"))
      end
   end
end

-- =============================================================================
-- Command: ldb zone [name]
-- =============================================================================
function ldb_zone(name, line, wildcards)
   if not require_db() then return end
   local zone_name = wildcards[1]
   if not zone_name or zone_name == "" then
      zone_name = cached_zone
      if not zone_name then
         ColourNote("red", "", PREFIX .. "Current zone unknown. Try again after GMCP data arrives.")
         return
      end
   end
   show_zone_stats(zone_name)
end

function show_zone_stats(zone_name)
   local like_pattern = "%" .. zone_name .. "%"

   local zones_found = {}
   for row in db:nrows(string.format(
      "SELECT DISTINCT zone FROM kills WHERE zone LIKE %s", fixsql(like_pattern))) do
      table.insert(zones_found, row.zone)
   end

   if #zones_found == 0 then
      ColourNote("yellow", "", PREFIX .. "No kills recorded in zones matching '" .. zone_name .. "'.")
      return
   end

   for _, zone in ipairs(zones_found) do
      ColourNote("cyan", "", PREFIX .. "Zone: " .. zone)

      for row in db:nrows(string.format(
         "SELECT COUNT(*) as cnt, COALESCE(SUM(xp_gained), 0) as xp " ..
         "FROM kills WHERE zone = %s ", fixsql(zone))) do

         if row.cnt > 0 then
            ColourNote("white", "", string.format("  Kills: %s | XP: %s",
               format_number(row.cnt), format_number(row.xp)))

            local avg_xp = math.floor(row.xp / row.cnt)
            ColourNote("white", "", string.format("  Avg XP/kill: %s", format_number(avg_xp)))
         end
      end

      -- Deaths in this zone
      for row in db:nrows(string.format(
         "SELECT COUNT(*) as cnt FROM deaths WHERE zone = %s", fixsql(zone))) do
         if row.cnt > 0 then
            ColourNote("red", "", "  Deaths: " .. row.cnt)
         end
      end

      -- Level range
      for row in db:nrows(string.format(
         "SELECT MIN(level) as min_lvl, MAX(level) as max_lvl " ..
         "FROM kills WHERE zone = %s ", fixsql(zone))) do
         if row.min_lvl then
            ColourNote("white", "", string.format("  Level range: %d - %d", row.min_lvl, row.max_lvl))
         end
      end

      -- Top mobs
      ColourNote("cyan", "", "  Top mobs:")
      local count = 0
      for row in db:nrows(string.format(
         "SELECT mob_name, COUNT(*) as cnt, SUM(xp_gained) as xp " ..
         "FROM kills WHERE zone = %s " ..
         "GROUP BY mob_name ORDER BY cnt DESC LIMIT 5", fixsql(zone))) do
         count = count + 1
         ColourNote("white", "", string.format("    %d. %s (%d kills, %s XP)",
            count, row.mob_name, row.cnt, format_number(row.xp)))
      end
   end
end

-- =============================================================================
-- Command: ldb mob <name>
-- =============================================================================
function ldb_mob(name, line, wildcards)
   if not require_db() then return end
   local mob_search = wildcards[1]
   local like_pattern = "%" .. mob_search .. "%"

   ColourNote("cyan", "", PREFIX .. "Mob search: " .. mob_search)

   local found = false
   for row in db:nrows(string.format(
      "SELECT mob_name, COUNT(*) as cnt, COALESCE(SUM(xp_gained), 0) as xp, " ..
      "COALESCE(SUM(damage_total), 0) as dmg, " ..
      "COALESCE(SUM(rounds), 0) as rnd " ..
      "FROM kills WHERE mob_name LIKE %s " ..
      "GROUP BY mob_name ORDER BY cnt DESC LIMIT 10", fixsql(like_pattern))) do

      found = true
      ColourNote("white", "", string.format("  %s: %d kills, %s XP",
         row.mob_name, row.cnt, format_number(row.xp)))

      local avg_xp = math.floor(row.xp / row.cnt)
      local avg_rnd = row.cnt > 0 and string.format("%.1f", row.rnd / row.cnt) or "0"
      ColourNote("white", "", string.format("    Avg XP: %s | Avg rounds: %s",
         format_number(avg_xp), avg_rnd))

      -- Zones where this mob was killed
      local zones = {}
      for zrow in db:nrows(string.format(
         "SELECT DISTINCT zone FROM kills WHERE mob_name = %s ",
         fixsql(row.mob_name))) do
         table.insert(zones, zrow.zone)
      end
      if #zones > 0 then
         ColourNote("white", "", "    Zones: " .. table.concat(zones, ", "))
      end
   end

   if not found then
      ColourNote("yellow", "", "  No kills found matching '" .. mob_search .. "'.")
   end
end

-- =============================================================================
-- Command: ldb top mobs [N]
-- =============================================================================
function ldb_top_mobs(name, line, wildcards)
   if not require_db() then return end
   local limit = tonumber(wildcards[1]) or 10

   ColourNote("cyan", "", PREFIX .. "Top " .. limit .. " mobs by kill count:")

   local count = 0
   for row in db:nrows(string.format(
      "SELECT mob_name, COUNT(*) as cnt, COALESCE(SUM(xp_gained), 0) as xp " ..
      "FROM kills " ..
      "GROUP BY mob_name ORDER BY cnt DESC LIMIT %d", limit)) do
      count = count + 1
      ColourNote("white", "", string.format("  %d. %s (%d kills, %s XP)",
         count, row.mob_name, row.cnt, format_number(row.xp)))
   end

   if count == 0 then
      ColourNote("yellow", "", "  No kills recorded.")
   end
end

-- =============================================================================
-- Command: ldb top zones [N]
-- =============================================================================
function ldb_top_zones(name, line, wildcards)
   if not require_db() then return end
   local limit = tonumber(wildcards[1]) or 10

   ColourNote("cyan", "", PREFIX .. "Top " .. limit .. " zones by total XP:")

   local count = 0
   for row in db:nrows(string.format(
      "SELECT zone, COUNT(*) as cnt, COALESCE(SUM(xp_gained), 0) as xp " ..
      "FROM kills " ..
      "GROUP BY zone ORDER BY xp DESC LIMIT %d", limit)) do
      count = count + 1
      ColourNote("white", "", string.format("  %d. %s (%d kills, %s XP)",
         count, row.zone, row.cnt, format_number(row.xp)))
   end

   if count == 0 then
      ColourNote("yellow", "", "  No kills recorded.")
   end
end

-- =============================================================================
-- Command: ldb top xp [N]
-- =============================================================================
function ldb_top_xp(name, line, wildcards)
   if not require_db() then return end
   local limit = tonumber(wildcards[1]) or 10

   ColourNote("cyan", "", PREFIX .. "Top " .. limit .. " mobs by avg XP per kill:")

   local count = 0
   for row in db:nrows(string.format(
      "SELECT mob_name, COUNT(*) as cnt, SUM(xp_gained) as xp, " ..
      "CAST(SUM(xp_gained) AS REAL) / COUNT(*) as avg_xp " ..
      "FROM kills " ..
      "GROUP BY mob_name HAVING cnt >= 3 " ..
      "ORDER BY avg_xp DESC LIMIT %d", limit)) do
      count = count + 1
      ColourNote("white", "", string.format("  %d. %s (avg %s XP, %d kills)",
         count, row.mob_name, format_number(math.floor(row.avg_xp)), row.cnt))
   end

   if count == 0 then
      ColourNote("yellow", "", "  No mobs with 3+ kills recorded.")
   end
end

-- =============================================================================
-- Command: ldb deaths [N]
-- =============================================================================
function ldb_deaths(name, line, wildcards)
   if not require_db() then return end
   local limit = tonumber(wildcards[1]) or 10

   ColourNote("cyan", "", PREFIX .. "Last " .. limit .. " deaths:")

   local count = 0
   for row in db:nrows(string.format(
      "SELECT timestamp, mob_name, zone, level, tier, remort, redo FROM deaths " ..
      "ORDER BY timestamp DESC LIMIT %d", limit)) do
      count = count + 1
      local time_str = os.date("%Y-%m-%d %H:%M", row.timestamp)
      local mob = (row.mob_name and row.mob_name ~= "") and row.mob_name or "unknown"
      local tr = ""
      if row.tier and row.remort then
         tr = " [" .. format_tier_label(row.tier, row.redo) .. "R" .. row.remort .. "]"
      end
      ColourNote("white", "", string.format("  %s - Lvl %-3d - %-25s (%s)%s",
         time_str, row.level, mob, row.zone, tr))
   end

   if count == 0 then
      ColourNote("yellow", "", "  No deaths recorded.")
   end
end

-- =============================================================================
-- Command: ldb db
-- =============================================================================
function ldb_db_info(name, line, wildcards)
   ColourNote("cyan", "", PREFIX .. "Database info:")
   ColourNote("white", "", "  Path: " .. (db_path or "(none)"))

   if not db then
      ColourNote("yellow", "", "  Database not open.")
      return
   end

   -- File size
   local f = io.open(db_path, "r")
   if f then
      local size = f:seek("end")
      f:close()
      if size < 1024 then
         ColourNote("white", "", "  Size: " .. size .. " bytes")
      elseif size < 1024 * 1024 then
         ColourNote("white", "", string.format("  Size: %.1f KB", size / 1024))
      else
         ColourNote("white", "", string.format("  Size: %.1f MB", size / (1024 * 1024)))
      end
   end

   -- Record counts
   for row in db:nrows("SELECT COUNT(*) as cnt FROM kills") do
      ColourNote("white", "", "  Kills: " .. format_number(row.cnt))
   end
   for row in db:nrows("SELECT COUNT(*) as cnt FROM deaths") do
      ColourNote("white", "", "  Deaths: " .. format_number(row.cnt))
   end
   for row in db:nrows("SELECT COUNT(*) as cnt FROM quests") do
      ColourNote("white", "", "  Quests: " .. format_number(row.cnt))
   end
   for row in db:nrows("SELECT COUNT(*) as cnt FROM campaigns") do
      ColourNote("white", "", "  Campaigns: " .. format_number(row.cnt))
   end
   for row in db:nrows("SELECT COUNT(*) as cnt FROM gquests") do
      ColourNote("white", "", "  GQuests: " .. format_number(row.cnt))
   end
end

-- =============================================================================
-- Plugin lifecycle
-- =============================================================================
function OnPluginInstall()
   enabled = (GetVariable("enabled") == "true")
   open_db()

   -- Read current GMCP state (may have been broadcast before plugin load/reload)
   local base = gmcp("char.base")
   if base then
      if base.perlevel then cached_perlevel = tonumber(base.perlevel) or 1000 end
      if base.tier then cached_tier = tonumber(base.tier) end
      if base.remorts then cached_remorts = tonumber(base.remorts) end
      if base.redos then cached_redos = tonumber(base.redos) end
      if base.pups then cached_pups = tonumber(base.pups) end
   end
   last_pup_event_id = nil  -- Clear stale pup train tracking on reload

   local status = gmcp("char.status")
   if status then
      local new_level = tonumber(status.level)
      local new_tnl = tonumber(status.tnl)
      if new_level and new_level ~= -1 then cached_level = new_level end
      if new_tnl and new_tnl ~= -1 then cached_tnl = new_tnl end
      cached_enemy = status.enemy or ""
   end

   local room = gmcp("room.info")
   if room then
      cached_zone = room.zone
      cached_room_num = tonumber(room.num)
      cached_room_name = room.name
   end

   -- Restore active quest/campaign IDs from saved state
   local saved_quest = GetVariable("active_quest_id")
   if saved_quest and saved_quest ~= "" then
      active_quest_id = tonumber(saved_quest)
   end
   local saved_campaign = GetVariable("active_campaign_id")
   if saved_campaign and saved_campaign ~= "" then
      active_campaign_id = tonumber(saved_campaign)
   end
   local saved_gquest = GetVariable("active_gquest_id")
   if saved_gquest and saved_gquest ~= "" then
      active_gquest_id = tonumber(saved_gquest)
   end

   -- Check quest status via GMCP (handler already sent request quest on connect)
   local quest_data = gmcp("comm.quest")
   if quest_data then
      handle_quest(quest_data)
   end

   -- Schedule delayed cp info / gq info checks to detect/reconcile in-progress activities
   if enabled then
      DoAfterSpecial(2, "delayed_cp_info()", 12)  -- 12 = sendto.script
      DoAfterSpecial(3, "delayed_gq_info()", 12)  -- 3s to avoid overlap with cp info
      ColourNote("cyan", "", PREFIX .. "Loaded (enabled).")
   else
      ColourNote("cyan", "", PREFIX .. "Loaded (disabled). Use 'ldb on' to start collecting data.")
   end
end

function OnPluginSaveState()
   SetVariable("enabled", tostring(enabled))
   SetVariable("active_quest_id", active_quest_id and tostring(active_quest_id) or "")
   SetVariable("active_campaign_id", active_campaign_id and tostring(active_campaign_id) or "")
   SetVariable("active_gquest_id", active_gquest_id and tostring(active_gquest_id) or "")
end

function OnPluginClose()
   close_db()
end

function OnPluginDisconnect()
   if in_combat then
      end_combat()
   end
end
]]>
</script>
</muclient>
